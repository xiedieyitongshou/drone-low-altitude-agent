# 低空作业气象决策 Agent 项目面试指南

## 1. 项目一句话介绍

这是一个面向无人机低空任务的气象决策 Agent。系统接入和风天气数据，把地点、逐小时天气、天气预警转换成内部统一模型，再通过规则引擎判断任务是否适飞，并进一步支持推荐执行窗口、多地点比选、历史复盘、自然语言入口、RAG 建议、会话记忆和 Docker 部署。

面试中可以这样概括：

> 我做的不是一个天气查询接口，而是一个低空任务决策系统。天气 API 只是数据来源，核心价值在于把外部天气数据标准化后，结合任务类型和行业规则，输出可解释、可复盘、可扩展的任务执行建议。

## 2. 总体架构怎么讲

当前主链路是：

```text
自然语言输入 / 结构化请求
  ↓
输入解析与校验
  ↓
天气 Provider 获取原始数据
  ↓
Mapper 转换成内部统一模型
  ↓
规则引擎评估风险
  ↓
推荐 / 比选 / 历史复盘
  ↓
RAG 检索操作建议
  ↓
统一响应与解释层输出
```

最重要的分层：

- `Provider`：只负责对接外部天气 API。
- `Mapper`：把外部不同格式转换为内部统一结构。
- `Schema`：定义输入、天气、预警、评估结果的数据契约。
- `Rules`：只基于内部模型做确定性判断。
- `Service / Orchestrator`：组织业务流程，不写死规则细节。
- `Memory / DB`：保存会话上下文、用户偏好、对话历史和评估历史。

面试重点：

> 这个项目一直在做边界收口。外部 API 的不确定性收在 Provider 和 Mapper，用户输入的不确定性收在 Parser 和 Schema，业务判断的不确定性收在规则配置里。这样后续更换天气源、接入前端或接入大模型时，不需要推翻主链路。

## 3. 天气服务和 Mapper 怎么讲

天气服务层负责三件事：

- 地点解析
- 逐小时天气查询
- 天气预警查询

但规则引擎不直接消费和风天气原始响应，而是通过 mapper 转成内部模型：

- `WeatherDataBundle`
- `WeatherHourData`
- `WarningDataBundle`
- `WarningData`

为什么要这么做：

- 和风天气的 Geo、Hourly、Warning 接口返回结构不同。
- 第三方字段可能变化，不能让规则引擎直接依赖外部字段名。
- 后续如果换数据源，只优先改 Provider / Mapper。

面试中可以这样答：

> 我把和风天气看作外部 Provider，内部业务不直接依赖它的原始 JSON。Provider 负责请求，Mapper 负责适配，规则引擎只消费统一模型。这样数据源变化不会直接污染核心业务逻辑。

## 4. 规则引擎怎么讲

规则引擎是项目的核心。它完成三步：

1. 根据逐小时天气做小时级判断。
2. 根据天气预警做风险修正。
3. 根据时间段内最差小时汇总整体结论。

输出结果包括：

- `适飞`
- `慎飞`
- `禁飞`
- 逐小时风险原因
- 时间段整体风险原因

任务类型通过 `mission_profiles` 配置，不同任务使用不同阈值：

- `cruise`：巡航
- `inspection`：巡检
- `hover`：悬停拍摄
- `survey`：测绘

面试重点：

> 规则引擎不依赖自然语言，也不依赖 RAG，更不依赖大模型。它是确定性的安全判断模块。大模型和 RAG 只能做解析、建议和表达增强，不能替代规则结论。

如果被问“规则是否行业可靠”，可以这样答：

> 当前规则是基于已有天气 API 能提供的字段做的工程化风险判断，包括风速、降水、能见度、湿度和预警等级。它不是民航级适航标准，而是面向低空巡航场景的业务规则原型。后续如果接入更完整的数据源，可以扩展指标和阈值，但规则引擎结构不需要重写。

### 风速规则怎么讲

和风天气返回的 `windSpeed` 按 `km/h` 理解，规则配置中已经把字段语义从 `ms` 改为 `kmh`，避免单位混乱。

当前风速分档：

- `windSpeed < 15 km/h`：不单独因风速触发风险
- `15 <= windSpeed < 25 km/h`：慎飞
- `windSpeed >= 25 km/h`：禁飞

风力等级处理：

- 单独 `1级`、`2级`、`3级` 保留 provider 原值
- `1-3级` 视为低风区间，归一化为 `2级`
- `3-4级`、`4-5级` 等其他区间仍按上限判断

面试说法：

> 这里的关键不是简单套阈值，而是先统一单位，再处理 provider 区间值。和风天气的 `windSpeed` 是 km/h，所以规则配置也改成 km/h；`1-3级` 不是确定的 3 级风，直接取上限会过度保守，因此归一到中间值 2 级，而单独的 3 级仍按 3 级处理。

## 5. 推荐、比选和历史复盘怎么讲

第三周以后，系统从“能判断”升级成“能辅助决策”。

### 推荐窗口

作用：

- 扫描传入的逐小时天气数据。
- 按时间顺序线性扫描逐小时评估结果。
- 遇到禁飞小时或时间断点就切分连续可执行窗口。
- 每个连续段只生成一个推荐窗口，避免重复推荐重叠时间段。
- 按适飞优先、风险低优先、连续性优先排序。

可以回答：

```text
什么时候最适合执行任务？
```

### 多地点比选

作用：

- 对多个地点并行评估。
- 按可飞小时数、最长连续可飞小时、最早可飞时间、窗口质量打分。
- 返回排序和推荐地点。

可以回答：

```text
多个地点中先去哪一个？
```

### 历史复盘

作用：

- `/cruise/evaluate` 自动落库。
- 保存请求、地点、天气快照、预警快照和评估结果。
- 通过 `request_id` 查询历史记录。

面试重点：

> 推荐和比选没有重新写一套判断逻辑，而是复用规则引擎的小时级结果。历史复盘保存的是结构化评估过程，不是简单保存最终文本，所以后续可以追溯当时为什么判定为慎飞或禁飞。

## 6. Agent、Memory 和 LLM 预留怎么讲

### 自然语言入口

当前 `/agent/query` 是主入口，负责：

- 解析自然语言
- 判断意图：评估、推荐、比选
- 调用对应业务服务
- 组织统一响应
- 保存会话上下文和对话记录

当前自然语言解析采用：

- 关键词
- 正则
- `dateparser`
- 上下文补全

边界说明：

> 当前解析方案适合规整表达，不是完整语义理解。后续已预留 LLM 结构化解析模块，但默认不启用付费 API。

### Session Memory

作用：

- 保存短期上下文。
- 支持“那明天下午呢”这类省略表达。

当前实现：

- 本地默认 `TTLCache`
- Docker 环境可切 Redis
- 通过 `SESSION_MEMORY_BACKEND` 配置切换

面试说法：

> TTLCache 适合本地单进程开发，Redis 适合 Docker 和多进程部署。两者通过统一接口切换，业务层不关心底层存储。

### Profile Memory

当前是单用户版本：

- 默认 `user_id=default_user`
- `user_profiles` 表保存默认任务偏好和常用地点
- 后续接入登录后，把默认用户替换为真实用户 ID

注意：

> 当前不是完整用户系统，因为没有前端登录和认证。它是预留用户维度的单用户记忆系统。

### Conversation History

作用：

- 保存每次 `/agent/query` 的自然语言请求、解析结果、响应摘要和完整响应。
- 返回 `conversation_id`。

面试重点：

> Redis 只做短期缓存，对话历史和用户偏好必须落数据库。这样才适合长期复盘、审计和后续用户画像。

### LLM 解释层

当前实现：

- `llm_client.py` 预留统一大模型客户端
- `llm_task_parser.py` 预留大模型解析
- `response_explainer.py` 先用模板生成中文解释
- 默认 `LLM_ENABLED=false`

面试重点：

> 我没有让大模型参与安全判断，而是把它放在解析和表达增强层。当前为了控制成本使用模板解释器，后续可以接 OpenAI 兼容 API、百炼、Gemini 或 Groq。

## 7. RAG 怎么讲

当前 RAG 的定位是“建议增强”，不是“决策引擎”。

当前实现：

- 知识库文件：`data/knowledge/advice_rules.json`
- 检索方式：`TF-IDF + cosine similarity`
- 接口：`POST /knowledge/advice/retrieve`
- 输出：风险说明、操作建议、知识片段

为什么不用 RAG 直接判断：

- 低空飞行风险判断需要稳定、可复现。
- RAG 检索结果可能受知识片段和 query 影响。
- 规则引擎更适合输出硬结论，RAG 更适合补充建议。

面试中可以这样答：

> 当前 RAG 不改变 `适飞 / 慎飞 / 禁飞` 结论，只根据规则结果召回相关建议。这样既保留确定性，又能展示知识增强能力。后续如果预算允许，可以把 TF-IDF 升级成本地 embedding + FAISS / Chroma，或者接云端 embedding API。

如果被问“TF-IDF 是否太简单”，可以这样答：

> 是的，TF-IDF 是最低可运行版本。我先用它跑通知识库、索引、检索和接口结构。后续真正增强会优先考虑本地中文 embedding 模型加 FAISS，这样不依赖付费 API，也比词频检索更接近语义检索。

## 8. 数据库和 Docker 怎么讲

### 数据库

当前使用：

- SQLite
- SQLAlchemy
- Alembic

已有持久化内容：

- 历史评估记录
- 天气快照
- 预警快照
- 用户偏好
- 对话历史

为什么先用 SQLite：

> SQLite 部署简单，适合本地开发和求职演示。ORM 和 Alembic 已经接入，后续迁移 PostgreSQL 主要是换 `DATABASE_URL` 和处理少量数据库差异，业务代码不用大改。

### Docker

当前 Docker Compose 包含：

- `frontend`：React 前端，Nginx 托管静态资源
- `app`：FastAPI 后端
- `redis`：Session Memory 缓存
- `./data:/app/data`：SQLite 数据挂载

已验证：

- `docker compose up --build -d` 成功
- 前端 `http://localhost:5173` 可访问
- `/health` 返回 `environment=docker`
- `/docs` 可访问
- Redis 中能看到 session key
- 多轮自然语言解析能复用 Redis 上下文
- 后端容器内可以运行核心测试，例如推荐窗口测试

面试说法：

> 本地 Docker 可以看作服务器部署前的演练。它验证了前端静态托管、FastAPI 后端、Redis、SQLite 挂载、环境变量和端口映射能否在 Linux 容器环境中协同运行。真正服务器部署主要多出公网 IP、安全组、域名、HTTPS、反向代理和重启策略。

## 9. 前端展示怎么讲

目前项目后端能力已经比较完整，但只展示 Swagger 不够直观，所以第六周开始补一个轻量前端控制台。Day 36 已经完成前端工程骨架搭建。

前端定位：

- 不重新实现业务逻辑
- 只消费后端 API
- 用页面展示 Agent 决策结果

Day 36 已完成：

- 使用 `React + Vite + TypeScript` 初始化 `frontend/`
- 使用 `axios` 封装后端请求
- 使用 `react-router-dom` 建立页面路由
- 新增系统状态页，请求后端 `/health`
- 预留后续业务页面入口

Day 37 联调补充：

- 新增 Agent 对话页，调用 `/agent/query`
- 支持传入 `session_id` 和 `user_id`
- 展示 `explanation`、`context_used`、`conversation_id`
- 折叠展示 `parsed`、`composed`、`result`
- 解决前后端跨域导致的 `Network Error` 和 `OPTIONS 405`

Day 38-41 已完成：

- 单地点评估页：调用 `/cruise/evaluate`，展示整体结论、逐小时评估、风险原因、天气预警和 `request_id`
- 推荐窗口页：调用 `/cruise/recommend`，展示推荐窗口排名、时间段、风险分数和推荐理由
- 多地点比选页：调用 `/cruise/compare`，展示推荐地点、地点排名、综合得分、可飞小时数和最长连续可飞小时
- 历史复盘页：调用 `/cruise/history/{request_id}` 和 `/cruise/history/{request_id}/composed`，展示历史请求、评估结果和统一解释
- 逐小时表格补充展示 `降水概率`，避免只有降水量但看不到概率风险来源

### 前后端 CORS 问题怎么讲

本地联调时，前端运行在 `http://localhost:5173`，后端运行在 `http://localhost:8000`。虽然都在本机，但端口不同，浏览器会把它们视为不同源。

表现：

- `/health` 后端日志返回 `200`，但前端显示 `Network Error`
- `/agent/query` 请求前，浏览器先发 `OPTIONS` 预检请求
- 后端没有 CORS 配置时，`OPTIONS /agent/query` 返回 `405 Method Not Allowed`

修正：

- 在 FastAPI 中加入 `CORSMiddleware`
- 通过 `CORS_ALLOWED_ORIGINS` 配置允许前端来源
- 默认允许 `http://localhost:5173` 和 `http://127.0.0.1:5173`

面试说法：

> 这个问题不是业务接口失败，而是浏览器同源策略导致的跨域拦截。后端虽然能正常返回数据，但浏览器不允许前端 JS 读取响应。对于 `POST /agent/query`，浏览器还会先发 `OPTIONS` 预检请求，所以没有 CORS 中间件时会看到 `405`。我通过 FastAPI 的 `CORSMiddleware` 显式放行前端来源，解决了本地前后端联调问题。

计划页面：

- Agent 对话
- 单地点评估
- 推荐窗口
- 多地点比选
- 历史查询

技术选型：

- React
- Vite
- TypeScript
- axios
- react-router-dom

面试说法：

> 前端不重新实现业务判断，只负责组织用户输入、调用后端 API、展示 Agent 决策结果和处理交互状态。当前前端已经覆盖系统状态、Agent 对话、单地点评估、推荐窗口、多地点比选和历史复盘，能完整演示从输入、判断、推荐、比选、落库到复盘的主链路。

## 10. 高频追问回答

### 为什么不用大模型直接判断？

因为低空飞行安全判断必须稳定可复现。大模型适合做自然语言解析、结果解释和建议生成，不适合直接替代规则引擎输出安全结论。

### 为什么要加 Mapper？

因为第三方 API 返回格式不稳定，而且不同接口结构不同。Mapper 把外部数据转换成内部统一模型，规则引擎只依赖内部结构，后续换数据源不会大改核心逻辑。

### 为什么 Redis 不存用户偏好？

Redis 是短期缓存，适合会话上下文；用户偏好和对话历史需要长期保存，应该放数据库。

### 为什么当前 Profile Memory 是单用户？

项目目前没有前端登录和认证。当前用 `default_user` 跑通数据模型和业务闭环，后续接入真实登录后替换为真实 `user_id`。

### 为什么先用 SQLite？

SQLite 简单稳定，适合本地演示。项目已经用 SQLAlchemy 和 Alembic 隔离数据库访问，后续迁移 PostgreSQL 的成本可控。

### Docker 化解决了什么？

解决环境一致性问题。前端、后端、Redis、数据库挂载和迁移流程都能通过 Compose 一键启动，为服务器部署和项目演示做准备。

### 为什么 `/health` 返回 200，前端还显示 Network Error？

这是 CORS 问题。后端日志里的 `200` 只能说明接口本身返回成功，但如果响应头没有允许前端来源，浏览器会拦截响应，前端代码拿不到数据。修复方式是在 FastAPI 中配置 `CORSMiddleware`，允许 Vite 前端地址访问后端。

### 为什么 `/agent/query` 会出现 `OPTIONS 405`？

因为跨域 `POST` 请求会触发浏览器预检。浏览器先发 `OPTIONS /agent/query`，确认后端是否允许跨域提交。如果后端没有 CORS 中间件，FastAPI 没有对应的 `OPTIONS` 路由，就会返回 `405 Method Not Allowed`。

## 11. 最终综合表达

可以直接背这段：

> 这个项目从一个天气判断原型重构成了低空任务决策 Agent。我的核心设计是把外部天气 API、内部数据模型、规则引擎、推荐比选、历史复盘、Agent 编排、RAG 建议和部署能力拆开。规则引擎负责稳定结论，RAG 负责建议增强，大模型只作为可选的解析和表达层。数据上通过 SQLite 保存任务历史、用户偏好和对话记录，通过 TTLCache / Redis 保存短期会话上下文。部署上已经用 Docker Compose 跑通前端、FastAPI 后端和 Redis，前端可以完整演示自然语言入口、评估、推荐、比选和历史复盘。

---

## DeepSeek 自然语言解析增强怎么讲

### 这次增强解决了什么问题

原来的自然语言入口主要依赖关键词、正则和 `dateparser`，优点是可控、低成本、离线可运行，但对真实中文表达的泛化能力有限。例如：

- 地点容易混入动词和修饰语。
- “哪个窗口”可能被误判成多地点比选。
- “那换成测绘呢”这类省略表达需要更多上下文规则。
- 停用词和正则会随着样例增加而膨胀。

因此我在 Day46-Day50 增加了 DeepSeek 结构化解析层，用大模型只做一件事：把用户自然语言转成后端已有规则引擎能消费的结构化参数。

### 当前链路

```text
用户自然语言
  -> DeepSeek LLM Parser
  -> Pydantic Schema 校验
  -> Agent Orchestrator
  -> evaluate / recommend / compare
  -> 规则引擎 / 推荐服务 / 比选服务
  -> 统一响应
```

### 为什么不是让大模型直接判断能不能飞

低空任务安全判断必须稳定、可复现、可解释，所以大模型不参与最终安全决策。DeepSeek 只负责解析：

- `intent`：`evaluate` / `recommend` / `compare`
- `location` 或 `locations`
- `date`
- `start_time` / `end_time`
- `task_type`
- `scan_hours` 等推荐参数

最终能不能飞、是否慎飞、是否禁飞，仍然由规则引擎根据天气、预警和任务类型阈值判断。

### 为什么保留规则解析 fallback

我没有把系统强绑定到 DeepSeek。当前支持三种模式：

```env
NL_PARSER_MODE=rule
NL_PARSER_MODE=llm
NL_PARSER_MODE=hybrid
```

其中求职演示推荐使用：

```env
NL_PARSER_MODE=hybrid
```

这样 DeepSeek 可用时走 LLM 解析；如果模型超时、余额不足、返回非法 JSON 或网络失败，就自动回退到原来的规则解析。这样做的好处是：

- 项目本地仍可离线运行。
- 演示时不会因为模型 API 抖动导致主链路完全不可用。
- 面试时能体现工程稳定性和降级设计。

### Docker 中怎么配置 DeepSeek

真实 API Key 不写进 `Dockerfile`，也不提交 GitHub，而是放在 `.env`：

```env
LLM_ENABLED=true
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-v4-flash
DEEPSEEK_API_KEY=your_deepseek_api_key
NL_PARSER_MODE=hybrid
```

`docker-compose.yml` 负责把这些环境变量注入后端容器。`Dockerfile` 只保留非敏感默认值，例如默认关闭 LLM。

### 面试表达

可以这样说：

> 我把自然语言解析层设计成可替换模块。默认规则解析保证可控和低成本，DeepSeek 解析用于提升中文输入的泛化能力。模型只输出结构化任务参数，所有输出都会经过 Pydantic 校验；如果解析失败，hybrid 模式会回退到规则解析。低空飞行安全结论仍然由确定性规则引擎负责，LLM 不直接参与安全判断。

### 已验证能力

Docker 环境下已经验证：

- 单地点评估：`深圳湾明天下午2点到5点可以飞吗`
- 窗口推荐：`深圳未来72小时最佳执行窗口是什么时候`
- 多地点比选：`深圳湾、南山区、宝安机场附近明天下午哪个更适合先巡检`

响应中可以通过 `parser_source` 判断解析来源：

- `llm`：DeepSeek 解析成功
- `llm_fallback_rule`：DeepSeek 失败后回退到规则解析
- `rule`：只使用规则解析