[pytest]
testpaths = tests
python_files = test_*.py
markers =
    eval_fast: quick Agent/RAG eval regression gates for local development and CI
    eval_full: slower full eval suites intended for manual release checks
