



﻿﻿# 低空作业气象决策 Agent 开发计划与时间表

## 1. 计划说明

本计划基于 [项目重构技术方案](D:\desktop\YYagent\项目重构技术方案.md) 制定，目标是将当前原型重构为一个可本地开发、可服务器部署、可上传 GitHub 的低空作业气象决策 Agent 求职项目。

时间假设：
- 每天可投入 `4-5 小时`
- 以 `5 周` 为一个完整开发周期
- 目标不是一次性堆满技术点，而是每周都产出可运行、可展示、可写进简历的阶段成果

开发优先级：
1. 先做核心主链路
2. 再做功能增强
3. 再做 Agent 能力
4. 最后做 RAG、部署和包装

---

## 2. 总体阶段划分

## Phase 1：项目骨架与基础后端
目标：
- 完成仓库初始化
- 跑通基础后端服务
- 明确目录结构和模块边界

预计时间：
- 第 1 周

关键交付物：
- GitHub 仓库初始化
- FastAPI 项目骨架
- 基础配置、日志、环境变量
- 天气 API 封装骨架

## Phase 2：规则引擎本地化
目标：
- 将百炼中的核心判断逻辑迁移为 Python 模块
- 形成可单元测试的规则引擎

预计时间：
- 第 2 周

关键交付物：
- 逐小时天气提取模块
- 预警提取模块
- 巡航/低空作业判断模块
- 单元测试

## Phase 3：功能增强
目标：
- 增加最佳执行时间推荐
- 增加多地点任务比选
- 增加历史任务复盘
- 增加任务类型差异化规则

预计时间：
- 第 3 周

关键交付物：
- 推荐窗口模块
- 多地点比选模块
- 历史记录落库
- 任务类型配置体系

## Phase 4：Agent 化与 Memory
目标：
- 增加自然语言输入解析
- 增加会话记忆和用户记忆
- 增加统一任务编排

预计时间：
- 第 4 周

关键交付物：
- Agent Orchestrator
- Session Memory
- Profile Memory
- 统一响应格式

## Phase 5：RAG、部署与求职包装
目标：
- 增加解释层增强
- 完成 Docker 部署
- 输出 README、架构图、演示材料

预计时间：
- 第 5 周

关键交付物：
- RAG 检索链路
- Docker 部署
- README
- 演示截图与项目说明

## Phase 6：前端展示与 LLM 解析增强
目标：
- 补齐前端展示控制台
- 完成前后端 Docker 集成
- 接入 DeepSeek 自然语言结构化解析

预计时间：
- 第 6 周到第 8 周

关键交付物：
- React 前端展示控制台
- 前端 Docker 集成
- DeepSeek Hybrid Parser
- LLM 解析 fallback 机制

## Phase 7：多用户体系与运营审计
目标：
- 将单用户 Agent Demo 升级为多用户业务系统
- 完成登录认证、数据隔离、长期记忆持久化
- 增加轻量管理员审计能力

预计时间：
- 第 9 周到第 10 周

关键交付物：
- 用户登录与 JWT 鉴权
- Conversation History 按用户隔离
- Session Memory 持久化
- Profile Memory 绑定真实用户
- 管理员用户管理、任务审计和统计页面

---

## 3. 每日开发安排

## 第 1 周：项目骨架与后端基础

### Day 1：仓库初始化与技术选型落地
目标：
- 搭建代码仓库骨架
- 明确目录结构
- 初始化 Git

当天任务：
- 新建项目目录结构
- 初始化 `README.md`
- 初始化 `requirements.txt` 或 `pyproject.toml`
- 初始化 `.env.example`
- 创建 `app/`、`tests/`、`docs/`、`scripts/`、`docker/`
- 提交第一次 commit

完成标准：
- 项目 clone 后可以直接看懂结构

### Day 2：FastAPI 服务启动
目标：
- 让后端服务先跑起来

当天任务：
- 安装 FastAPI、Uvicorn、Pydantic（FastAPI：Python 的 Web API 框架，用来写后端接口。Uvicorn：ASGI 服务器。它不负责“写接口”，它负责“把你写的 FastAPI 应用跑起来”。Pydantic：数据校验和数据模型库。它负责把请求参数、响应结构、配置项定义成清晰的 Python 模型，并自动校验类型。）

三者怎么配合
你写 FastAPI 应用
用 Pydantic 定义请求/响应/配置模型
用 Uvicorn 启动服务
可以理解成：
FastAPI = 框架
Pydantic = 数据规则
Uvicorn = 运行服务器

- 编写基础 `main.py`
- 提供 `/health` 接口
- 加入基础配置和环境变量读取
- 加入统一日志初始化

完成标准：
- 本地可通过 `uvicorn` 启动服务
- `/health` 返回成功

### Day 3：天气服务模块搭建
目标：
- 搭建天气 API 封装层

当天任务：
- 新建 `services/weather/`
- 封装地理位置解析接口
- 封装逐小时天气接口
- 封装天气预警接口
- 统一 HTTP 请求、超时、重试和异常处理

完成标准：
- 代码层面能独立调用三个天气接口

### Day 4：Schema 与配置结构设计
目标：
- 统一输入输出结构

当天任务：
- 定义请求参数 Schema
- 定义天气数据 Schema
- 定义预警数据 Schema
- 定义巡航判断输出 Schema
- 规划数据库表结构草案

完成标准：
- 所有主链路数据结构清晰、可复用

### Day 5：输入校验与错误处理
目标：
- 做到“输入合法才能进入主流程”

当天任务：
- 校验日期格式
- 校验时间段格式
- 增加跨天时间段处理
- 定义统一错误响应格式
- 补充接口级异常处理

完成标准：
- 非法输入能稳定返回错误信息

### Day 6：联调天气主链路
目标：
- 打通“输入 -> 天气获取”

当天任务：
- 串起地点解析与天气查询
- 验证经纬度、逐小时天气、预警能否正常返回
- 加入调试日志
- 保存真实接口返回样例到 `data/samples/`

完成标准：
- 输入地点和时间后，后端能拿到完整原始天气数据

### Day 7：第一周收尾
目标：
- 固定基础层

当天任务：
- 清理重复代码
- 补充注释
- 修复边界问题
- 写阶段总结到 `docs/`

完成标准：
- 基础服务稳定，进入规则引擎开发

---

## 第 2 周：规则引擎本地化

### Day 8：逐小时天气提取模块
目标：
- 将脚本节点逻辑迁移为 Python 模块

当天任务：
- 编写时间段解析逻辑
- 支持 `24:00` 和跨天
- 从逐小时天气中提取目标时间段小时桶
- 输出统一 `hourly_weather`

完成标准：
- 单独调用函数能得到正确的小时天气列表

### Day 9：预警提取模块
目标：
- 标准化预警数据

当天任务：
- 从原始预警数据提取 `event_type`、`warning_level` 等字段
- 输出统一 `warnings`
- 兼容无预警情况

完成标准：
- 预警格式稳定，便于下游判断

### Day 10：巡航规则引擎迁移
目标：
- 将当前巡航条件判断代码拆成独立模块

当天任务：
- 编写逐小时判定函数
- 编写预警修正函数
- 编写时间段汇总函数
- 保留当前实际阈值与规则

完成标准：
- 本地规则结果与原型一致

### Day 11：规则配置化
目标：
- 降低硬编码程度

当天任务：
- 将阈值提取到配置文件
- 支持任务类型差异化阈值
- 为禁飞/慎飞条件增加可读配置注释

完成标准：
- 修改规则不再必须改业务函数

### Day 12：规则引擎单元测试
目标：
- 提高可信度

当天任务：
- 编写天气规则测试
- 编写预警修正测试
- 编写跨天时间段测试
- 编写空数据测试

完成标准：
- 核心规则引擎具备可运行测试

### Day 13：接口化巡航判断
目标：
- 提供可直接调用的评估 API

当天任务：
- 新增 `/cruise/evaluate`
- 接收地点、日期、时间段、任务类型
- 返回逐小时评估和整体结论

完成标准：
- Postman 或前端可直接调这个接口

### Day 14：第二周收尾
目标：
- 让核心判断闭环稳定

当天任务：
- 对照原型输出检查一致性
- 清理规则文档
- 补充开发说明

完成标准：
- 第 2 周结束时，核心业务闭环完成

---

## 第 3 周：功能增强

### Day 15：最佳执行时间推荐设计
目标：
- 明确推荐窗口算法

当天任务：
- 定义推荐窗口输出结构
- 设定连续窗口最小时长
- 设定排序策略：适飞优先、风险低优先、连续性优先

完成标准：
- 推荐逻辑设计清晰可编码

### Day 16：最佳执行时间推荐实现
目标：
- 完成时间窗口推荐模块

当天任务：
- 扫描未来 72 小时逐小时数据
- 识别连续低风险窗口
- 输出推荐时间段与原因

完成标准：
- 系统可回答“什么时候最适合执行任务”

### Day 17：多地点任务比选设计与实现
目标：
- 支持批量地点评估

当天任务：
- 定义多地点输入结构
- 实现多地点并行天气评估
- 输出风险排序和推荐地点

完成标准：
- 系统可回答“多个地点中先去哪一个”

### Day 18：历史任务复盘落库
目标：
- 让系统具备任务回看能力

当天任务：
- 建立查询历史、天气快照、判断结果表
- 保存每次任务输入与输出
- 支持按时间或用户检索历史记录

完成标准：
- 历史任务可追溯

具体实施步骤

1.设计最小落库表
2.接 SQLite + SQLAlchemy + Alembic
3.先让 /cruise/evaluate 自动落库
4.再做一个历史查询接口
5.最后再决定要不要扩到推荐/比选结果

### Day 19：任务类型差异化规则
目标：
- 让系统支持多个低空任务模板

当天任务：
- 定义任务类型配置：巡航、巡检、悬停拍摄、测绘
- 为不同任务定义不同阈值
- 接入规则引擎

完成标准：
- 不同任务得到不同风险结论

### Day 20：功能增强联调
目标：
- 检查新增四项功能是否稳定

当天任务：
- 测试推荐窗口
- 测试多地点排序
- 测试历史记录保存与读取
- 测试不同任务类型

完成标准：
- 新增功能均可单独演示

### Day 21：第三周收尾
目标：
- 固化增强功能

当天任务：
- 清理代码
- 更新文档
- 记录后续 Agent 化输入输出规范

完成标准：
- 第 3 周结束时，系统从“判断工具”升级为“任务决策系统”

---

## 第 4 周：Agent 化与 Memory

### Day 22：自然语言输入解析
目标：
- 支持自然语言任务请求

当天任务：
- 设计输入格式
- 编写输入解析模块
- 解析地点、日期、时间段、任务类型、多地点列表

完成标准：
- 用户可直接说一句自然语言请求

### Day 23：Orchestrator 编排器
目标：
- 串起完整任务链路

当天任务：
- 编写主编排逻辑
- 串起输入解析、天气服务、规则引擎、推荐服务、比选服务、响应生成
- 增加失败兜底

完成标准：
- 一个入口即可完成完整任务调用

### Day 24：Session Memory
目标：
- 支持会话上下文继承

当天任务：
- 用 Redis 或本地缓存实现会话记忆
- 存地点、日期、时间段、任务类型
- 支持“那明天下午呢”这类省略表达

完成标准：
- 系统能复用上一轮上下文

### 之后再做的内容：Profile Memory 与长期用户体系
说明：
- 这一部分建议放到第五周核心内容完成后再做
- 原因是它已经不只是“再加一层记忆”，而是开始涉及真实业务系统中的用户体系
- 更合理的实现方式应与 `user_id`、用户角色、组织、权限、长期偏好、对话持久化一起设计

当前建议：
- 先保留接口设计和表结构思路
- 当前阶段不强行在第四周塞进完整实现
- 如需最小原型，可只做 `user_id + preference table + default parameter fill`

### Day 25：Profile Memory（延后实现）
目标：
- 增强真实业务属性

当天任务：
- 设计用户偏好表
- 记录常用地点、常用任务、输出偏好
- 支持默认参数补全

完成标准：
- 用户查询体验更接近真实业务系统

### Day 26：统一响应生成模块
目标：
- 输出统一业务响应

当天任务：
- 定义 response composer
- 输出结论、风险原因、推荐窗口、多地点排序、历史摘要
- 为后续 LLM 总结做准备

完成标准：
- 前端和 LLM 都能稳定消费响应

### Day 27：第四周联调
目标：
- 检查 Agent 主链路稳定性

当天任务：
- 做多轮输入测试
- 测试上下文继承
- 测试多功能组合调用
- 修复边界 case

完成标准：
- Agent 形态初步成立

### Day 28：第四周收尾
目标：
- 固化接口与结构

当天任务：
- 清理冗余函数
- 补充注释和文档
- 准备进入 RAG 和部署阶段

完成标准：
- 第 4 周结束时，Agent 主流程稳定可演示

---

## 第 5 周：RAG、部署与展示

### Day 29：知识库资料整理
目标：
- 为解释层提供资料来源

当天任务：
- 整理行业规则、任务规范、风险建议文档
- 切分为知识片段
- 规范标题、标签、来源

完成标准：
- 有可用知识库文档集

### Day 30：RAG 检索链路
目标：
- 让解释更专业

当天任务：
- 接入向量库
- 建立索引
- 完成按结论检索风险说明和操作建议

完成标准：
- 可根据判断结果检索相关规范片段

### Day 31：大模型解析与 LLM 解释层集成
目标：
- 将自然语言解析升级为大模型结构化解析
- 用模型整理最终业务说明

当天任务：
- 引入统一大模型客户端
- 保留 Day 22 的关键词/正则解析作为 fallback
- 使用大模型结构化输出解析地点、日期、时间段、任务类型、多地点列表
- 定义解释层 Prompt
- 组合规则结果、RAG 片段、建议条目
- 生成用户可读文本

完成标准：
- 自然语言解析可走大模型结构化输出
- 大模型失败时仍可回退到 Day 22 规则解析
- 最终输出不再只是 JSON

## 4. 后续延后实现内容

以下内容不影响当前主链路闭环，但从系统完整性看是明确的后续增强项。其中 4.1 和 4.2 已完成接口预留，暂未接入实际大模型 API。

### 4.1 自然语言解析升级（已完成接口预留）
- 当前版本：`规则 + 关键词 + 正则 + dateparser`
- 后续版本：在 Day 31 与 LLM 解释层一起引入大模型结构化解析
- 建议原则：保留当前输入输出结构，新增大模型解析器，Day 22 规则解析器作为 fallback
- 推荐依赖：`openai` + `pydantic`，可选 `instructor`

### 4.2 大模型统一接入方案（已完成接口预留）
- 目标：把自然语言解析升级和 LLM 解释层放到同一天处理
- 统一入口：新增 `llm_client.py`，避免自然语言解析和解释生成各接一套模型调用
- 推荐能力拆分：
  - `parse_task_request(...)`：负责自然语言到结构化参数
  - `generate_final_response(...)`：负责基于规则结果、RAG 片段、建议条目生成用户可读说明
- 推荐依赖：
  - 必选：`openai`、`pydantic`
  - 可选：`instructor`、`tenacity`
- 设计原则：
  - Day 22 规则解析器继续保留为 fallback
  - 大模型只负责结构化解析和表达生成，不替代规则引擎判断
  - 规则结果仍然是最终结论来源

### 4.3 RAG 检索升级方案
- 当前版本：`scikit-learn TF-IDF + cosine similarity`
- 当前定位：最低可运行 RAG 检索原型，适合先跑通知识库、索引、检索、建议返回流程
- 后续推荐方案一：`OpenAI Embeddings + FAISS`
  - 优点：真正语义检索、本地向量索引、工程量可控、适合求职项目展示
  - 推荐依赖：`openai`、`faiss-cpu`、`numpy`
  - 适合阶段：当前知识库规模较小、暂时不想引入数据库级向量存储时
- 后续推荐方案二：`OpenAI Embeddings + Chroma`
  - 优点：更接近现成向量库，支持 collection、metadata、持久化管理
  - 推荐依赖：`openai`、`chromadb`
  - 适合阶段：希望展示完整向量库能力，并且能接受更重依赖时
- 建议原则：
  - 先保持当前 TF-IDF 版本跑通完整链路
  - 等第五周全部功能稳定后，再把 `vector_knowledge_store.py` 替换为 embedding + 向量库实现
  - 保持 `/knowledge/advice/retrieve` 接口不变，减少对上层业务的影响

### 4.4 Session Memory 升级
- 当前版本：`TTLCache` 进程内缓存
- 后续版本：Redis 版会话记忆
- 建议原则：保持 `session_memory.py` 抽象层不变，只替换底层存储实现

### 4.5 Profile Memory 与用户体系
- 当前阶段：延后
- 后续版本：与 `user_id`、角色、组织、权限、长期偏好一起设计
- 建议原则：不要把长期用户体系和当前会话记忆混成一层

### 4.6 对话内容与用户偏好持久化
- 后续可增加对话记录表
- 后续可增加用户偏好表
- 后续可支持历史偏好学习、默认参数补全、输出风格偏好

### Day 32：Docker 化
目标：
- 为部署做准备

当天任务：
- 编写 Dockerfile
- 编写 docker-compose
- 配置数据库和缓存服务

完成标准：
- 本地可通过 Docker Compose 启动完整环境

### Day 33：服务器部署
目标：
- 跑到线上

当天任务：
- 准备 Linux 服务器
- 部署后端、数据库、Redis
- 配置 Nginx 反向代理
- 验证公网访问

完成标准：
- 有可访问的在线版本

### Day 34：GitHub 整理
目标：
- 把仓库做成求职项目

当天任务：
- 重写 `README.md`
- 增加架构图
- 增加 API 示例
- 增加部署说明
- 增加项目亮点说明

完成标准：
- GitHub 仓库具备展示价值

### Day 35：演示材料与收尾
目标：
- 为求职展示做准备

当天任务：
- 录制演示视频或 GIF
- 输出项目说明
- 输出答辩讲稿
- 检查代码和文档一致性

完成标准：
- 可直接用于面试讲解和作品展示

---

## 第 6 周：前端展示控制台

### Day 36：前端项目初始化
目标：
- 搭建可运行的前端工程

当天任务：
- 创建 `React + Vite + TypeScript` 项目
- 配置 `VITE_API_BASE_URL`
- 建立基础目录结构
- 增加 API 请求封装
- 增加 `/health` 连通性检查

完成标准：
- 前端项目可启动
- 页面能访问后端 `/health`

### Day 37：Agent 对话页面
目标：
- 展示统一自然语言入口能力

当天任务：
- 编写自然语言输入框
- 支持 `session_id` 和 `user_id`
- 调用 `/agent/query`
- 展示 `explanation`
- 展示 `parsed`、`composed`、`result` 折叠 JSON
- 展示 `context_used` 和 `conversation_id`

完成标准：
- 可以通过页面演示自然语言输入和多轮上下文继承

### Day 38：单地点评估页面
目标：
- 展示规则引擎确定性评估能力

当天任务：
- 编写地点、日期、时间段、任务类型表单
- 调用 `/cruise/evaluate`
- 展示整体结论
- 展示逐小时评估结果
- 展示风险原因、天气预警和 `request_id`

完成标准：
- 可以通过页面完成单地点任务风险评估

### Day 39：推荐窗口页面
目标：
- 展示最佳执行时间推荐能力

当天任务：
- 编写推荐窗口表单
- 调用 `/cruise/recommend`
- 展示推荐窗口列表
- 展示排名、时间段、风险分数和推荐理由
- 处理无可飞窗口情况

完成标准：
- 系统可通过页面回答“什么时候最适合执行任务”

### Day 40：多地点比选页面
目标：
- 展示多个候选地点排序能力

当天任务：
- 编写多地点输入表单
- 调用 `/cruise/compare`
- 展示地点排名
- 展示综合得分、可飞小时数、最长连续可飞小时
- 展示推荐地点和排序原因

完成标准：
- 系统可通过页面回答“多个地点中先去哪一个”

### Day 41：历史查询与复盘页面
目标：
- 展示任务历史持久化与复盘能力

当天任务：
- 编写 `request_id` 查询输入
- 调用 `/cruise/history/{request_id}`
- 调用 `/cruise/history/{request_id}/composed`
- 展示历史任务请求、评估结果和统一解释
- 展示 `composed.details.advice` 和 `knowledge_snippets`，让 RAG 建议在页面中显性可见
- 增加 loading、错误提示和空结果状态

完成标准：
- 可以完整演示一次任务从输入、判断、知识库建议、落库到复盘

### Day 42：第六周联调与体验整理
目标：
- 固化前端展示主链路

当天任务：
- 联调 Agent、评估、推荐、比选、历史查询页面
- 统一结论卡片、风险列表、表格和 JSON 展示样式
- 检查 Agent 与历史复盘页面中的 RAG 建议展示
- 整理前端演示用例
- 修复字段不一致和异常展示问题

完成标准：
- 前端可以完整展示后端核心能力

---

## 第 7 周：前端 Docker 与求职展示收尾

### Day 43：前端 Docker 集成
目标：
- 前后端可通过 Docker 同时启动

当天任务：
- 为前端补充 Dockerfile
- 在 `docker-compose.yml` 中增加 `frontend` 服务
- 配置前端访问后端 API 地址
- 验证本地 Docker 同时启动前端、后端、Redis

完成标准：
- 本地可通过 Docker Compose 启动完整展示环境

### Day 44：前端文档与部署说明
目标：
- 补齐前端使用和部署文档

当天任务：
- 编写前端 README
- 更新总 README 的前端展示说明
- 补充前后端 Docker 启动说明
- 记录 Vercel / Netlify / Render Static Site 的可选部署方式

完成标准：
- 仓库文档能说明如何启动和展示前端页面

### Day 45：演示脚本与作品包装
目标：
- 为面试展示准备完整材料

当天任务：
- 整理演示路径
- 准备典型测试输入
- 准备项目亮点说明
- 检查 GitHub 文件结构
- 更新面试指南中的前端展示话术

完成标准：
- 项目可以通过前端页面完成完整演示

---

## 第 8 周：DeepSeek 自然语言解析增强

### 本周定位
目标：
- 在不改变规则引擎的前提下，用 DeepSeek API 增强自然语言解析能力
- 将用户自然语言稳定转换为规则引擎可消费的结构化参数
- 保留当前关键词/正则解析作为 fallback，避免大模型 API 不可用时主链路失效

设计原则：
- DeepSeek 只负责 `自然语言 -> 结构化任务参数`
- 规则引擎仍然负责 `适飞 / 慎飞 / 禁飞` 等安全结论
- Pydantic 负责校验模型输出，非法输出不能直接进入业务链路
- 所有模型调用通过统一客户端封装，便于后续切换 Qwen、OpenAI 兼容 API 或本地模型
- 20 元 DeepSeek 余额主要用于开发和演示，避免高频无意义调用

推荐配置：
```env
LLM_ENABLED=true
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
DEEPSEEK_API_KEY=your_api_key_here
NL_PARSER_MODE=hybrid
LLM_TIMEOUT_SECONDS=20
```

推荐链路：
```text
用户自然语言
  -> DeepSeek 结构化解析
  -> Pydantic Schema 校验
  -> 缺参补问 / fallback 到规则解析
  -> Agent Orchestrator
  -> evaluate / recommend / compare
  -> 规则引擎输出最终业务结论
```

### Day 46：DeepSeek 接入方案设计
目标：
- 明确大模型解析层的输入、输出、错误处理和 fallback 规则

当天任务：
- 梳理当前 `nl_parser.py` 的输出结构：`intent`、`target_endpoint`、`parsed`、`warnings`、`context_used`
- 设计 DeepSeek 解析输出 JSON Schema
- 明确三类意图的必要字段：
  - `evaluate`：`location`、`date`、`start_time`、`end_time`、`task_type`
  - `recommend`：`location`、`date`、`scan_hours`、`min_window_hours`、`task_type`
  - `compare`：`locations`、`date`、`start_time`、`end_time`、`top_k`、`task_type`
- 明确缺参策略：模型只标记缺失字段，不编造地点和时间
- 明确 fallback 策略：模型调用失败、JSON 解析失败、Schema 校验失败时回退到规则解析

完成标准：
- 有清晰的 LLM 解析输入输出契约
- 不修改规则引擎和下游业务服务

### Day 47：统一 LLM Client 实现
目标：
- 封装 DeepSeek API 调用，避免业务代码直接依赖 HTTP 细节

当天任务：
- 新增 `app/services/llm_client.py`
- 使用 OpenAI 兼容接口调用 DeepSeek
- 从环境变量读取 `DEEPSEEK_API_KEY`、`LLM_MODEL`、`LLM_TIMEOUT_SECONDS`
- 增加超时、异常、空响应处理
- 增加基础日志，但不打印 API Key 和完整敏感请求
- 在 `.env.example` 中补充 DeepSeek 相关配置项

完成标准：
- 后端可以通过一个函数完成 DeepSeek 调用
- API Key 不写死在代码里
- LLM Client 可以被自然语言解析器复用

### Day 48：LLM Task Parser 实现
目标：
- 用 DeepSeek 把用户自然语言解析为结构化任务参数

当天任务：
- 新增 `app/services/llm_task_parser.py`
- 编写结构化解析 Prompt
- 要求模型只返回 JSON，不返回解释性文本
- 支持会话上下文输入，用于处理“那明天下午呢”“换成测绘呢”这类省略表达
- 将 DeepSeek 输出转换为当前 `ParsedTaskRequest`
- 对日期、时间、任务类型、地点列表做二次校验

完成标准：
- DeepSeek 能解析单地点评估、窗口推荐、多地点比选三类输入
- 输出结构与当前 orchestrator 兼容
- 模型输出异常时不会污染业务链路

### Day 49：Hybrid Parser 接入 Orchestrator
目标：
- 在 `/agent/query` 主链路中接入 DeepSeek 解析，但保持规则解析可用

当天任务：
- 在 `task_orchestrator.py` 中增加 parser 选择逻辑
- 支持 `NL_PARSER_MODE=rule`：只使用当前规则解析
- 支持 `NL_PARSER_MODE=llm`：只使用 DeepSeek 解析
- 支持 `NL_PARSER_MODE=hybrid`：优先 DeepSeek，失败后 fallback 到规则解析
- 在响应中通过 `parser_source` 标识 `rule`、`llm` 或 `llm_fallback_rule`
- 在 `warnings` 中提示是否发生 fallback

完成标准：
- 默认本地仍可不依赖 DeepSeek 运行
- 开启 `hybrid` 后，Agent 页面可以直接使用 DeepSeek 增强解析
- 下游 `/cruise/evaluate`、`/cruise/recommend`、`/cruise/compare` 不需要改动

### Day 50：解析测试与成本控制
目标：
- 验证 DeepSeek 解析稳定性，并控制 token 消耗

当天任务：
- 新增自然语言解析测试样例集
- 覆盖以下输入：
  - 规整表达：`深圳明天下午2点到5点可以飞吗`
  - 复杂表达：`帮我看看深圳湾明天下午适不适合做低空测绘`
  - 推荐表达：`深圳未来72小时最佳执行窗口是什么时候`
  - 比选表达：`深圳湾、南山区、宝安机场附近明天下午哪个更适合先巡检`
  - 省略表达：`那换成后天上午呢`
  - 缺参表达：`帮我看看明天能不能飞`
- 给 LLM 调用增加简单缓存或开发期手动样例，避免重复烧 token
- 记录一次请求的大致 token 消耗和调用成本
- 增加 `.env.example` 中的关闭开关说明

完成标准：
- 至少 15 条自然语言样例可以稳定解析
- 明确哪些场景由 LLM 解析，哪些场景 fallback
- 20 元 DeepSeek 余额足够支撑开发、联调和面试演示

### Day 51：前端 Agent 页面展示增强
目标：
- 让前端可以清楚展示当前自然语言解析来源和 fallback 情况

当天任务：
- 在 Agent 对话页展示 `parser_source`
- 在 Agent 对话页展示 `warnings`
- 增加 3 个演示按钮：
  - 单地点评估示例
  - 推荐窗口示例
  - 多地点比选示例
- 优化错误提示：区分“模型解析失败”“缺少字段”“下游天气 API 失败”
- 保留 JSON 折叠展示，方便面试时说明结构化解析结果

完成标准：
- 面试演示时可以直观看到自然语言输入被解析成哪些规则数据
- 可以展示 DeepSeek 解析和 fallback 机制

### Day 52：文档、面试话术与收尾
目标：
- 将 DeepSeek 解析增强包装成清晰的求职项目亮点

当天任务：
- 更新 README 中的自然语言解析说明
- 更新项目面试指南中的 LLM 边界说明
- 增加 DeepSeek 配置和启动说明
- 增加演示输入样例
- 补充“为什么不用大模型直接判断飞行安全”的回答
- 补充“为什么保留规则解析 fallback”的回答

完成标准：
- 文档能清楚说明 DeepSeek 在项目中的位置
- 面试时可以讲清楚：LLM 负责理解输入，规则引擎负责安全决策
- 项目具备“规则引擎 + LLM 解析 + RAG 建议 + Memory 上下文”的完整 Agent 叙事

---

## 第 9 周：多用户登录与记忆持久化

### 本周定位
目标：
- 将项目从 `default_user` 单用户演示模式升级为真实多用户体系
- 支持不同用户登录、身份识别和数据隔离
- 让 Conversation History、Session Memory、Profile Memory 与真实用户绑定
- 保持规则引擎、LLM 解析、RAG 建议等主链路不被用户体系耦合污染

设计原则：
- 用户体系只负责身份、权限和数据归属，不参与飞行安全判断
- 规则引擎仍然只接收结构化任务参数，不直接感知登录逻辑
- `/agent/query` 优先使用 token 中解析出的真实用户，不信任前端传入的 `user_id`
- Session Memory 解决短期上下文，Profile Memory 解决长期偏好，两者边界保持清晰
- 第一版支持 `user` 和 `admin` 两类轻量角色，不做复杂 RBAC 权限矩阵

推荐链路：
```text
用户登录
  -> 获取 access token
  -> 前端请求携带 Authorization
  -> 后端解析当前用户
  -> /agent/query 使用真实 user_id
  -> LLM / 规则解析自然语言
  -> 规则引擎生成业务结论
  -> 按 user_id 保存历史记录、会话上下文、长期偏好
  -> 用户只能查看自己的历史任务和 Profile
```

推荐环境变量：
```env
JWT_SECRET_KEY=replace-with-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
AUTH_DEMO_MODE=false
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=change-me-before-deploy
```

### Day 53：用户模型与数据库迁移
目标：
- 建立真实用户数据模型

当天任务：
- 新增 `User` ORM 模型
- `users` 表增加 `username`、`password_hash`、`display_name`、`role`、`is_active`、`created_at`、`updated_at`
- `role` 第一版支持 `user`、`admin`
- 检查并调整 `UserProfile`、`ConversationRecord` 与 `user_id` 的关系
- 新增 Alembic migration
- 初始化默认测试用户或管理员账号创建脚本

完成标准：
- 数据库中存在 `users` 表
- 用户与 Profile、Conversation History 可以建立关联
- 迁移可以从空库稳定执行

### Day 54：认证模块与 JWT 鉴权
目标：
- 建立后端登录认证基础能力

当天任务：
- 新增 `app/services/auth_service.py`
- 实现密码哈希和密码校验
- 实现 JWT access token 生成与解析
- 新增 `get_current_user` 依赖函数
- 新增 `require_admin_user` 依赖函数
- token 错误、过期、用户禁用时返回明确错误

完成标准：
- 明文密码不会落库
- 请求携带合法 token 时可以解析出当前用户
- 普通用户访问管理员接口返回 403
- token 错误或过期时返回 401

### Day 55：登录注册与当前用户接口
目标：
- 提供前后端可用的账号入口

当天任务：
- 新增 `POST /auth/register`
- 新增 `POST /auth/login`
- 新增 `GET /auth/me`
- 新增基础 Pydantic Schema
- 登录响应返回 access token 和基础用户信息
- `/auth/me` 返回当前用户 `id`、`username`、`display_name`、`role`、`is_active`

完成标准：
- 前端可以注册或登录普通用户
- 登录后返回 access token
- `/auth/me` 可以用于前端判断当前用户角色和页面入口

### Day 56：Agent 主链路接入真实用户
目标：
- 让自然语言任务与真实登录用户绑定

当天任务：
- 调整 `/agent/query`，优先使用 token 中的当前用户
- 保留 `user_id` 参数作为开发兼容，但生产链路不信任该参数
- `profile_memory` 使用 `current_user.id`
- `conversation_history` 使用 `current_user.id`
- 明确未登录访问策略：禁止访问或落到匿名 demo 用户

完成标准：
- 不同用户调用 `/agent/query` 时，历史记录分别归属到自己的账号
- 用户 A 的 Profile 不影响用户 B
- 规则引擎输入输出不受认证改造影响

### Day 57：历史记录按用户隔离
目标：
- 将历史复盘从 `request_id` 调试入口升级为用户历史列表和内容检索

当天任务：
- 新增 `GET /agent/conversations` 查询当前用户对话历史，支持 `keyword` 内容检索
- `keyword` 可匹配用户原始 `query`、系统摘要 `message`、最终解释 `explanation`
- 新增 `GET /agent/conversations/{conversation_id}` 查询单条记录，供前端点击历史列表后加载详情
- 调整历史任务相关接口，确保普通用户只能查询自己拥有的数据
- 增加分页参数：`page`、`page_size` 或 `limit`、`offset`
- 增加 `session_id`、`intent`、`parser_source` 筛选参数，方便按会话、意图和解析来源过滤
- 前端用户不需要手动输入 `conversation_id`，只通过关键词搜索和列表点击完成历史查询
- 后端继续保留 `conversation_id` 作为稳定详情定位 ID，便于接口调用、审计和排错

完成标准：
- 用户 A 不能查询用户 B 的对话记录
- 用户 A 不能通过关键词搜索到用户 B 的对话内容
- 用户 A 即使知道用户 B 的 `conversation_id`，查询详情也返回 404
- 历史列表能按时间倒序展示
- 历史列表支持按关键词检索自己的历史内容
- 前端历史页面不再依赖手动输入 `request_id` 或 `conversation_id` 才能查询

### Day 58：Session Memory 持久化
目标：
- 让会话记忆从临时缓存升级为可恢复会话能力

当天任务：
- 新增 `session_records` 表
- 字段包含 `id`、`user_id`、`title`、`last_context`、`created_at`、`updated_at`、`expires_at`
- 将当前 `session_memory_store` 抽象为接口
- 支持 TTLCache、Redis、Database 三种后端策略之一
- `/agent/query` 每次调用后更新当前 session 的 `last_context`

完成标准：
- 刷新页面后仍可恢复用户最近会话列表
- 同一个用户的不同 session 上下文互不污染
- 不同用户即使使用相同 `session_id`，也不会读取到彼此上下文

### Day 59：Profile Memory 管理接口
目标：
- 将长期用户偏好升级为真实用户可管理数据

当天任务：
- 新增 `GET /users/me/profile`
- 新增 `PATCH /users/me/profile`
- 支持用户编辑默认地点、默认任务类型、常用地点、输出偏好
- Orchestrator 继续自动从用户请求中更新 Profile
- 明确自动更新与手动配置的优先级
- 使用 DBeaver 连接当前数据库，检查 `users`、`user_profiles`、`conversation_records`、`session_records` 表结构和样例数据
- 记录 DBeaver 连接方式：SQLite 阶段连接 `data/drone_agent.db`，后续迁移 PostgreSQL 时复用同一工具管理连接

完成标准：
- 用户可以查看和修改自己的 Profile
- 自然语言省略地点时，可以从当前用户 Profile 中补全
- 用户 A 的 Profile 不影响用户 B
- 可以通过 DBeaver 查看用户、Profile、历史记录和 Session 数据是否按用户正确归属
- 明确 DBeaver 只用于数据查看、调试和人工核查，正式表结构变更仍通过 SQLAlchemy + Alembic migration 完成

---

## 第 10 周：多用户前端与管理员审计

### 本周定位
目标：
- 补齐多用户登录后的前端体验
- 为 `admin` 角色增加轻量账号管理、任务审计和系统统计能力
- 让普通用户和管理员看到不同页面、不同数据范围

设计原则：
- 前端根据 `/auth/me.role` 控制页面入口，但真正权限必须由后端接口校验
- 普通用户只能访问自己的历史、Session、Profile
- 管理员可以查看全局任务审计和用户状态，但不参与飞行安全判断
- 第一版管理员能力只做轻量运营后台，不做组织、多租户、菜单权限和复杂 RBAC

### Day 60：登录注册页面与请求鉴权
目标：
- 让前端具备完整登录状态

当天任务：
- 新增登录页和注册页
- 保存 access token
- 请求 API 时自动携带 `Authorization: Bearer <token>`
- 增加退出登录功能
- 未登录访问受保护页面时引导到登录页

完成标准：
- 未登录时不能直接进入 Agent 主页面
- 登录成功后进入 Agent 页面
- 退出后清理本地 token 和用户状态

### Day 61：用户历史与 Profile 页面升级
目标：
- 将前端历史与设置页改为当前用户视角

当天任务：
- 历史页面改为展示当前用户的历史任务列表
- 支持点击某条历史记录查看完整输入、解析结果、业务响应和最终 explanation
- 保留 `request_id` 查询能力作为调试入口
- 新增用户设置页，展示和编辑默认地点、默认任务类型、常用地点、输出偏好

完成标准：
- 当前用户可以看到自己的历史任务
- 当前用户可以维护自己的常用任务偏好
- Agent 页面省略信息时能体现 Profile 补全效果

### Day 62：管理员用户管理接口
目标：
- 建立轻量账号治理能力，明确管理员来源和权限边界
- 普通注册只产生普通用户，后续管理员只能由现有管理员授权产生
- 保留初始化第一个管理员的开发期方案，避免系统没有管理入口

当天任务：
- 新增管理员鉴权依赖，仅 `role=admin` 的用户可以访问 `/admin/*`
- 新增 `GET /admin/users`，支持按用户名、角色、启用状态筛选
- 新增 `PATCH /admin/users/{user_id}/status`，支持启用或禁用用户
- 新增 `PATCH /admin/users/{user_id}/role`，仅允许在 `user` 与 `admin` 之间切换
- 限制注册接口只能创建普通用户，不能通过注册直接创建管理员
- 提供初始化管理员方案，例如开发期种子脚本或初始化 SQL
- 防止禁用最后一个管理员账号
- 防止将最后一个管理员降级为普通用户

完成标准：
- 管理员可以查看用户列表
- 管理员可以启用或禁用普通用户
- 管理员可以将普通用户提升为管理员，也可以将非最后一个管理员降级为普通用户
- 普通用户不能访问任何 `/admin/*` 接口
- 注册接口不能创建管理员账号
- 被禁用用户不能继续登录或调用受保护接口
- 系统始终至少保留一个可用管理员账号

### Day 63：管理员任务审计接口
目标：
- 支持管理员跨用户查看任务历史

当天任务：
- 新增 `GET /admin/conversations`，查看全局历史任务
- 支持按 `user_id`、`session_id`、`intent`、`parser_source`、时间范围筛选
- 如果历史记录中已有风险等级或规则结论字段，支持按风险结果筛选
- 新增 `GET /admin/conversations/{conversation_id}`，查看单条任务完整详情

完成标准：
- 管理员可以跨用户查看任务历史
- 普通用户仍只能通过普通接口查看自己的历史
- 管理员审计查询默认分页
- 审计接口不允许修改历史任务内容

### Day 64：管理员统计与前端页面
目标：
- 让管理者看到系统运行情况

当天任务：
- 新增 `GET /admin/stats/tasks`
- 返回任务总量、用户数、活跃用户数、高风险任务数、解析失败数、规则拒绝数量
- 前端新增管理员入口，仅 `role=admin` 时展示
- 新增用户管理页，展示用户列表、角色、启用状态、创建时间
- 新增任务审计页，支持筛选、分页和查看详情

完成标准：
- 管理员登录后可以进入管理页面
- 普通用户前端不展示管理入口
- 普通用户即使直接访问管理路由也会被后端拒绝
- 统计页可以反映系统运行情况和异常任务分布

### Day 65：多用户与权限测试
目标：
- 验证身份、数据隔离和管理员权限边界

当天任务：
- 测试注册、登录、获取当前用户
- 测试未登录访问受保护接口返回 401
- 测试用户 A 不能访问用户 B 的历史记录
- 测试 `/agent/query` 能使用 token 用户写入历史
- 测试普通用户访问 `/admin/*` 返回 403
- 测试管理员可以访问用户列表和全局历史审计
- 测试禁用用户后，该用户不能继续调用受保护接口

完成标准：
- 普通用户数据隔离稳定
- 管理员权限边界清晰
- 核心 Agent 主链路不因认证和权限改造回退

### Day 66：文档、面试话术与收尾
目标：
- 将多用户与持久化能力包装成求职项目亮点

当天任务：
- 更新 README 中的账号体系、数据隔离和管理员能力说明
- 更新项目面试指南中的多用户、Memory 和权限边界话术
- 补充 `.env.example` 中 JWT 和初始管理员配置
- 准备普通用户和管理员两套演示账号
- 整理演示路径：登录、发起任务、查看我的历史、管理员审计、禁用用户

完成标准：
- 文档能说明系统如何从单用户 Demo 升级为多用户业务系统
- 面试时可以讲清楚：用户体系只管理身份和数据，不参与飞行安全判断
- 项目具备“规则引擎 + LLM 解析 + RAG 建议 + Memory + 多用户数据隔离 + 管理员审计”的完整产品化叙事

---

## 第 11 周：RAG 元数据、权限过滤与兜底

### 本周定位
目标：
- 将当前轻量 RAG 从“能检索建议”升级为“有知识类型、权限边界、地区提示和失败兜底”的企业 RAG 原型
- 明确个人求职项目不承诺实时政策库，只做政策提示和检索结构演示

设计原则：
- RAG 不参与适飞结论
- 政策提示不能包装成官方实时政策判断
- 权限过滤必须发生在后端检索层
- 召回为空或低置信时必须兜底，不能编造依据

### Day 67：知识库元数据结构升级
目标：
- 让知识库支持企业 RAG 常见 metadata 过滤

当天任务：
- 梳理 `data/knowledge/advice_rules.json` 当前字段
- 增加 `knowledge_type`，区分 `risk_advice`、`sop`、`policy_hint`、`faq`
- 增加 `region`、`province`、`city` 字段
- 增加 `visibility`、`tenant_id`、`user_id` 字段
- 增加 `version`、`effective_at`、`expires_at`、`review_status` 字段
- 更新 Pydantic schema，保证旧数据兼容

完成标准：
- 现有知识库仍能正常加载
- 每条知识可以表达“是什么知识、适用哪里、谁能看、是否过期”
- 文档中能说明这些字段对应企业 RAG 的权限和时效问题

### Day 68：地区政策提示样例
目标：
- 用少量样例展示地区政策知识库结构，不追求大规模真实数据

当天任务：
- 新增 3 到 5 条 `policy_hint` 类型知识
- 示例覆盖深圳、广州或通用低空政策提示
- 每条政策提示必须包含来源说明和“需官方核验”备注
- 避免写成确定政策裁决，例如“允许飞”或“禁止飞”
- 更新 RAG 返回结构，展示 `knowledge_type`、`region`、`source`、`version`

完成标准：
- RAG 可以返回地区政策提示
- 返回内容不会替代官方政策判断
- 面试时能讲清楚个人项目为什么不主打实时政策库

### Day 69：RAG 检索权限过滤
目标：
- 将用户隔离思想落到检索入口

当天任务：
- 设计 RAG 检索上下文：`user_id`、`tenant_id`、`role`、`region`、`task_type`
- 检索前先按 `visibility`、`tenant_id`、`user_id` 过滤候选文档
- 公共文档所有用户可见
- 租户文档仅当前租户可见
- 用户私有文档仅当前用户可见
- 保证权限过滤不依赖前端传参，而是来自后端当前用户

完成标准：
- 普通用户不能召回其他用户或其他租户私有知识
- 权限过滤逻辑有单元测试
- 面试时能回答“企业 RAG 如何防止知识库越权召回”

### Day 70：业务 metadata 过滤
目标：
- 提升 RAG 召回准确性，减少不相关知识进入候选集

当天任务：
- 检索前按 `task_type` 过滤任务类型
- 按 `region` 过滤地区政策提示
- 按 `risk_type` 匹配规则引擎输出风险标签
- 按 `review_status=approved` 排除未审核内容
- 按 `expires_at` 排除过期内容

完成标准：
- 深圳问题不会优先召回广州政策提示
- 高风速风险不会优先召回降水建议
- 未审核或过期知识不会进入正式召回结果

---

## 第 12 周：Agent Runtime 基础：Tool Registry、Agent State 与 Agent Loop

### Day 71：Tool Registry 设计

目标：

- 把当前固定 workflow 拆成可注册、可描述、可观测的工具集合
- 让后续 Agent 不再直接写死完整链路，而是按意图选择工具

当天任务：

- 梳理当前已有能力：规则评估、建议生成、知识检索、无人机推荐、飞行窗口推荐、历史查询
- 设计 `ToolSpec` 字段：`name`、`description`、`input_schema`、`output_schema`、`risk_level`、`timeout_ms`
- 设计 `ToolRegistry`，支持注册、查询、按名称调用
- 明确哪些工具是只读查询，哪些工具会产生业务状态变更

完成标准：

- 文档能说明“系统有哪些工具、每个工具解决什么业务问题”
- 代码中有清晰的工具注册入口
- 后续新增工具不需要改主链路的大量 if/else

---

### Day 72：核心业务工具注册

目标：

- 把现有核心能力接入 Tool Registry
- 保持原有接口兼容，不破坏前端和已有测试

当天任务：

- 注册风险评估工具
- 注册知识检索工具
- 注册无人机推荐工具
- 注册飞行窗口推荐工具
- 注册任务历史查询工具
- 为每个工具补充输入输出说明

完成标准：

- 至少 5 个核心工具可通过统一入口调用
- 旧的 `/agent/query` 接口仍能正常工作
- 工具调用失败时能返回结构化错误

---

### Day 73：Agent State 结构设计

目标：

- 引入 Agent 执行状态，支持多轮对话、追问和局部修改

当天任务：

- 设计 `AgentState`：用户输入、任务草稿、已确认字段、工具结果、缺失字段、执行轮次
- 设计 `AgentStep`：本轮选择的工具、输入、输出、状态变化
- 明确哪些字段来自用户，哪些字段来自系统推理，哪些字段来自工具返回
- 为后续 trace 和 eval 预留 `trace_id`、`run_id`

完成标准：

- Agent State 能表达一次任务从输入到工具调用再到最终回答的全过程
- 多轮追问不会丢失上一轮已确认信息
- 状态结构不依赖具体前端页面

---

### Day 74：规则 Planner 设计

目标：

- 先用可解释的规则 Planner 实现 Agent 决策，不急于引入复杂 LLM Planner

当天任务：

- 定义意图类型：查询、评估、推荐、比选、追问、修改、解释
- 根据意图选择工具集合
- 定义缺失字段检查规则
- 定义工具执行顺序：可并行、必须串行、可跳过
- 明确什么时候直接回答，什么时候进入工具调用

完成标准：

- 同一句自然语言入口可以走不同工具路径
- 不再默认固定执行完整链路
- Planner 的决策原因可以被记录和解释

---

### Day 75：最小 Agent Loop 原型

目标：

- 跑通“理解输入 → 规划工具 → 执行工具 → 更新状态 → 生成回答”的最小闭环

当天任务：

- 实现 `AgentLoop`
- 接入 `ToolRegistry`
- 接入 `AgentState`
- 支持最多 N 轮工具调用，避免无限循环
- 支持工具失败后的降级回答

完成标准：

- 简单查询只调用查询工具
- 风险评估只调用评估相关工具
- 推荐任务能按需调用评估、推荐和知识检索工具
- Loop 有最大轮次保护

---

### Day 76：兼容现有接口

目标：

- 在不重写前端的情况下，把后端执行核心切换到 Agent Runtime

当天任务：

- 保持 `/agent/query` 请求和响应字段兼容
- 新增可选 `debug` 或 `trace` 字段，便于展示工具调用路径
- 对比旧 workflow 与新 Agent Loop 的输出差异
- 补充兼容性测试

完成标准：

- 前端无需大改即可继续使用
- 旧测试不因接口变化失败
- 新响应能体现工具调用链路

---

### Day 77：第 12 周测试与文档

目标：

- 固化 Agent Runtime 基础能力，形成可面试讲解的工程边界

当天任务：

- 补充 Tool Registry 单元测试
- 补充 Agent State 多轮合并测试
- 补充 Agent Loop 路由测试
- 更新工程化改进计划和面试指南

完成标准：

- 能解释本项目从 workflow 升级为 Agent 的核心变化
- 能展示一次请求的工具选择和状态变化
- 第 12 周能力可独立演示

---

## 第 13 周：Trace、日志与工具失败恢复

### Day 78：Trace Event 结构设计

目标：

- 让每次 Agent 执行都有可追踪、可复盘的事件链路

当天任务：

- 设计 `TraceEvent`：事件类型、时间、输入摘要、输出摘要、耗时、错误信息
- 定义事件类型：plan、tool_call、tool_result、state_update、final_response、error
- 设计 `trace_id` 和 `run_id` 贯穿一次请求
- 明确哪些字段允许落库，哪些字段必须脱敏

完成标准：

- 一次 Agent 执行可以被拆解为多个 Trace Event
- Trace 能回答“为什么调用这个工具、工具结果是什么、最后怎么回答”

---

### Day 79：Tool Executor 与 Trace 接入

目标：

- 所有工具调用统一经过执行器，自动记录 trace

当天任务：

- 实现 `ToolExecutor`
- 封装工具调用前后的 trace 记录
- 记录工具耗时、成功/失败状态和错误类型
- 为工具设置超时和异常捕获

完成标准：

- 工具调用不再散落在业务代码里
- 任意工具失败都有结构化错误和 trace
- 后续 Agent Eval 可以直接复用 trace 数据

---

### Day 80：日志打印与脱敏

目标：

- 增加面向工程排障的日志，同时避免泄露用户敏感输入

当天任务：

- 统一日志格式：时间、trace_id、user_id、tenant_id、event_type、latency
- 对自然语言输入、地址、联系方式等字段做摘要或脱敏
- 区分开发日志和生产日志
- 补充日志开关配置

完成标准：

- 本地运行能看到清晰的 Agent 执行日志
- 日志不会直接打印完整敏感文本
- 面试时能讲清楚可观测性和隐私边界

---

### Day 81：工具失败分类

目标：

- 把工具失败从“报错”升级为可处理的业务状态

当天任务：

- 定义失败类型：参数缺失、权限不足、无结果、超时、外部依赖失败、内部异常
- 为每类失败设计用户可理解的返回话术
- 为可重试失败设计重试策略
- 为不可重试失败设计降级策略

完成标准：

- 工具失败不会直接中断整个 Agent
- 用户能知道下一步该补充什么或系统做了什么降级
- Trace 中能看出失败原因

---

### Day 82：失败恢复与降级回答

目标：

- Agent 在部分工具失败时仍能产出有价值回答

当天任务：

- 实现工具失败后的 fallback 分支
- 对 RAG 空召回、推荐无结果、规则评估缺字段分别处理
- 设计“需要追问”和“基于已有信息保守回答”的边界
- 增加失败恢复测试

完成标准：

- 单个工具失败不影响其他可用工具结果
- 缺字段时优先追问，而不是编造
- 空召回时明确说明知识库未命中

---

### Day 83：Trace 查询与历史兼容

目标：

- 支持后续前端展示和面试演示 Agent 执行轨迹

当天任务：

- 设计 trace 查询接口
- 将 trace 与任务历史关联
- 支持按 `trace_id` 查看工具调用链路
- 保持原有历史记录功能兼容

完成标准：

- 可以查询一次请求的完整执行轨迹
- 历史任务能关联到 Agent 执行过程
- 前端后续可直接展示工具调用过程

---

### Day 84：第 13 周测试与文档

目标：

- 完成 Agent 可观测性闭环

当天任务：

- 补充 trace 单元测试
- 补充工具失败恢复测试
- 更新日志与 trace 文档
- 更新面试指南中的 Agent 工程化问答

完成标准：

- 能通过 trace 解释一次 Agent 输出
- 能演示工具失败后的恢复策略
- 第 13 周能力可独立验收

---

## 第 14 周：混合型业务编排与多轮修改

### Day 85：意图体系重构

目标：

- 让同一句自然语言入口支持查询、评估、推荐、比选、追问和修改

当天任务：

- 重构意图分类规则
- 增加任务类型到工具路径的映射
- 明确单轮问答和任务型 Agent 的边界
- 补充典型用户输入样例

完成标准：

- 查询类问题不会触发完整评估链路
- 推荐类问题能按需组合多个工具
- 修改类问题能基于上一轮状态执行

---

### Day 86：查询类能力工具化

目标：

- 支持“只查询、不评估、不推荐”的轻量路径

当天任务：

- 增加任务历史查询工具
- 增加风险规则解释工具
- 增加知识库片段查询工具

完成标准：

- 用户问“为什么判高风险”时能解释规则来源
- 查询工具输出可被 Agent 直接组织成回答

---

### Day 87：多轮追问状态合并

目标：

- 支持缺少关键字段时先追问，再继续执行任务

当天任务：

- 实现缺失字段检测
- 实现用户补充信息合并到 `AgentState`
- 避免重复追问已确认字段
- 增加多轮追问测试

完成标准：

- 第一轮缺字段时不会强行给结论
- 第二轮补充字段后能继续调用工具
- 状态合并过程可通过 trace 查看

---

### Day 88：任务修改与受影响工具重跑

目标：

- 支持用户修改任务条件后，只重跑受影响工具

当天任务：

- 定义字段依赖关系：天气、区域、载重、距离、风险等级
- 判断哪些工具结果因字段修改失效
- 实现局部重跑策略
- 增加修改类对话测试

完成标准：

- 修改飞行区域后会重跑风险和知识检索
- 不再每次修改都完整重跑所有工具

---

### Day 89：RAG 作为可选工具接入

目标：

- RAG 不再是固定链路，而是 Agent 根据意图和置信度选择调用的工具

当天任务：

- 将知识检索封装为标准工具
- 支持按业务上下文传入 Day70 的 metadata 过滤条件
- 定义什么时候需要调用 RAG：政策解释、风险依据、操作建议、FAQ
- 定义什么时候不调用 RAG：纯历史查询、纯参数修改、简单推荐排序

完成标准：

- RAG 继续复用 Day67-Day70 的数据治理能力
- Agent 可以选择跳过 RAG
- RAG 输出能进入最终回答，但不会覆盖规则引擎结论

---

### Day 90：混合编排端到端测试

目标：

- 验证自然语言入口下不同意图走不同工具路径

当天任务：

- 编写查询、评估、推荐、比选、追问、修改 6 类 E2E 用例
- 校验每类用例的工具调用路径
- 校验最终回答是否引用正确工具结果
- 校验错误场景不会崩溃

完成标准：

- 至少 6 类典型用户问题通过测试
- 每类问题的工具调用链路符合预期
- Agent 行为不再像固定 workflow

---

### Day 91：第 14 周文档与演示

目标：

- 形成可展示的混合型业务编排能力

当天任务：

- 整理 3 条演示脚本
- 更新接口说明
- 更新面试指南中“workflow 与 agent 区别”的回答
- 补充架构图文字说明

完成标准：

- 能现场演示不同意图触发不同工具组合
- 能解释多轮修改为什么是 Agent State 能力
- 第 14 周能力可作为项目亮点

---

## 第 15 周：Guardrail、安全边界与 Agent 输出约束

### Day 92：Guardrail 模块设计

目标：

- 为 Agent 增加输入、工具调用和最终回答的安全边界

当天任务：

- 设计输入 guardrail：危险操作、越权查询、无关问题
- 设计工具 guardrail：高风险工具调用前检查权限和参数
- 设计输出 guardrail：避免绝对化承诺、避免无依据政策结论
- 定义拦截、追问、降级三类处理方式

完成标准：

- Guardrail 不只是敏感词过滤，而是覆盖 Agent 执行链路
- 高风险输出有明确约束
- 权限和安全边界可被测试

---

### Day 93：Guardrail 接入 Final Response

目标：

- 控制最终回答质量，降低幻觉和过度承诺

当天任务：

- 检查最终回答是否有工具依据
- 对政策、合规、风险结论增加保守表达
- 对缺少依据的结论触发降级或追问
- 增加 final response guardrail 测试

完成标准：

- Agent 不会把未召回内容说成确定政策
- 高风险建议会提示用户以官方要求为准
- 最终回答可解释、可约束

---

### Day 94：工具权限与越权拦截

目标：

- 防止 Agent 在多用户场景下绕过底层权限边界

当天任务：

- 为 ToolSpec 增加权限等级和访问条件
- 在 ToolExecutor 中统一检查用户等角色
- 对私有知识、企业知识、管理员工具分别设置调用边界
- 增加越权工具调用测试

完成标准：

- Agent 不能调用当前用户无权访问的工具或数据
- 工具权限失败能进入结构化错误和 trace
- Day67-Day70 的 RAG 数据隔离不会被 Agent Runtime 绕过

---

### Day 95：Guardrail Trace 与拒绝结果可解释

目标：

- 让 Guardrail 的拦截、降级和拒绝结果可追踪、可解释、可展示

当天任务：

- 统一 Guardrail 拒绝响应结构，区分输入拦截、工具拒绝和输出降级
- 在 trace 中记录 `guardrail_checkpoint`、`guardrail_action`、`violation_type` 和拒绝原因
- 将 Tool Guardrail 权限拒绝和 Output Guardrail LLM 降级结果整理为可读说明
- 文档说明 Guardrail Trace 如何支撑 Agent 可控性和问题排查

完成标准：

- 用户和开发者都能理解为什么请求被拒绝或降级
- Guardrail 事件能在 trace 中定位到具体检查点和原因
- 面试中能说明 Agent 不是黑盒执行，而是有边界、有记录、有兜底

---

### Day 96：安全与权限回归测试

目标：

- 验证租户隔离、用户隔离、工具权限和输出约束不会因 Agent 改造退化

当天任务：

- 回归 Day67-Day70 的 RAG metadata 过滤测试
- 增加工具权限测试
- 增加越权访问测试
- 增加安全输出测试

完成标准：

- 多租户与私有知识隔离仍然有效
- Agent 不会绕过底层检索权限
- Guardrail 与权限过滤都有自动化测试

---

### Day 97：风险输出模板与拒答策略

目标：

- 让高风险场景下的最终回答稳定、保守、可解释

当天任务：

- 设计风险评估、政策提示、飞行建议的输出模板
- 设计缺少依据时的拒答和追问策略
- 设计“系统无法替代官方审批”的固定边界说明
- 增加高风险输出快照测试

完成标准：

- 高风险回答不会给出过度确定结论
- 缺少依据时优先追问或说明限制
- 输出风格适合求职演示和工程面试解释

---

### Day 98：第 15 周测试与文档

目标：

- 完成 Agent 安全边界和输出约束闭环

当天任务：

- 运行核心权限与安全测试
- 更新 Guardrail 文档
- 更新面试指南中的安全边界问答
- 整理本周可演示案例

完成标准：

- Agent 的权限、输出、安全边界都能解释
- 第 15 周能力可独立验收

---

## 第 16 周：Hybrid RAG 检索增强：BM25、Embedding、融合排序与召回评估

### Day 99：RAG 检索架构重构

目标：

- 将当前 TF-IDF 检索升级为可扩展的多路召回架构

当天任务：

- 梳理现有 `LocalVectorKnowledgeStore` 和 `advice_retriever`
- 抽象 `Retriever` 接口：`retrieve(query, filters, top_k)`
- 将 metadata filter 固定为召回前置条件
- 保留 TF-IDF 作为 baseline retriever
- 设计 BM25、Embedding、Hybrid Retriever 的目录结构

完成标准：

- 检索模块不再绑定单一 TF-IDF 实现
- Day67-Day70 的权限、地域、时效过滤仍然是前置过滤
- 后续新增检索器不需要重写业务入口

---

### Day 100：BM25 关键词召回

目标：

- 补齐关键词精确匹配能力，解决纯向量检索容易漏掉编号、地名、政策名的问题

当天任务：

- 引入或实现轻量 BM25
- 为知识库构建 BM25 索引
- 明确 BM25 入库字段：标题、内容、risk_tags、task_type、region、policy source
- 支持按 metadata filter 后再进行 BM25 召回
- 增加政策名、城市名、风险词的召回测试

完成标准：

- 查询“深圳 管制空域”“实名登记”“禁飞区”等关键词能稳定命中
- BM25 索引可从本地知识库重建
- 能解释 BM25 解决的是关键词精确召回痛点

---

### Day 101：Embedding 向量召回

目标：

- 补齐语义召回能力，解决用户表达和知识库措辞不一致的问题

当天任务：

- 设计 embedding provider 接口，支持本地 mock 和后续真实模型替换
- 为知识切片生成或加载 embedding
- 实现向量相似度召回
- 支持 embedding 索引缓存和重建
- 增加同义表达、口语化提问的召回测试

完成标准：

- 用户不用完全命中关键词也能召回相关建议
- embedding provider 可替换，不把项目锁死在某个模型
- 能解释向量召回解决的是语义匹配痛点

---

### Day 102：Hybrid Retrieval 融合排序

目标：

- 用 BM25 + Embedding 融合排序提升召回稳定性

当天任务：

- 实现 BM25 与向量结果合并
- 设计融合分数：归一化分数、权重、重复文档合并
- 引入 metadata boost：同城市、同省、同任务类型、同风险标签加权
- 输出每条结果的召回来源和得分明细
- 增加 hybrid 排序测试

完成标准：

- 关键词强匹配和语义相关结果都能进入候选集
- 地域、任务类型、风险标签能影响排序
- Trace 中能看到结果来自 BM25、Embedding 还是两者共同命中

---

### Day 103：Chunk 策略与 Rerank

目标：

- 针对政策、SOP、FAQ、风险建议使用不同切片和重排策略

当天任务：

- 为 `policy_hint` 采用按条款或段落切片
- 为 `sop` 采用按步骤切片
- 为 `faq` 采用问答对切片
- 为 `risk_advice` 保留完整规则建议或短块切片
- 设计规则 rerank：有效期、review_status、地域精确度、知识类型优先级

完成标准：

- 不同知识类型有不同切片策略说明
- 召回结果不是只按文本相似度排序
- 能回答“不同场景下切片和召回做了哪些策略”

---

### Day 104：RAG 空召回、低置信与 Query Rewrite

目标：

- 处理用户提问但召回不准、召回不出的情况

当天任务：

- 定义空召回和低置信阈值
- 实现 query rewrite：补充任务类型、地区、风险标签等上下文
- 支持二次召回
- 二次召回仍失败时返回追问或保守说明
- 将 fallback 结果接入 Agent Loop 和 trace

完成标准：

- 空召回不会导致 Agent 编造政策
- 低置信结果不会被强行当作依据
- 能解释召回不准时的优化路径：改 query、调切片、改权重、补知识、做 eval

---

### Day 105：RAG Eval 与第 16 周文档

目标：

- 让 RAG 增强效果可量化，而不是只凭主观感觉

当天任务：

- 建立 RAG eval 样例：query、期望知识、期望引用、metadata 条件
- 统计 Recall@K、命中率、权限过滤正确率、低置信 fallback 通过率
- 对比 TF-IDF baseline、BM25、Embedding、Hybrid 的效果
- 更新面试指南中的 RAG 问答

完成标准：

- 有一条命令可以评估 RAG 召回质量
- 能量化说明 Hybrid RAG 比 TF-IDF 的改进
- RAG 作为 Agent 工具具备可解释、可评估、可迭代能力

---

## 第 17 周：Agent Eval 与 RAG Eval 质量评估

### Day 106：Agent Eval 与 RAG Eval 数据集设计

目标：

- 用小而精的数据集验证 Agent 决策质量和 RAG 召回质量，而不是只靠人工体验

当天任务：

- 设计评测样例格式：输入、历史状态、期望意图、期望工具、期望知识、期望结果要点
- 覆盖查询、评估、推荐、比选、追问、修改和知识检索
- 标注错误场景：缺字段、权限不足、空召回、工具失败
- 建立 `evals` 目录

完成标准：

- 有一套可版本管理的 Agent Eval 与 RAG Eval 样例
- 样例覆盖主要业务路径和异常路径
- 评测目标与岗位要求中的 Agent 质量保障对齐

---

### Day 107：Tool Calling Eval 脚本

目标：

- 自动评估 Agent 是否选对工具、少调用工具、不乱调用工具

当天任务：

- 编写 eval 脚本
- 统计工具选择准确率
- 统计多余工具调用率
- 统计缺失工具调用率
- 输出 markdown 或 json 报告

完成标准：

- 一条命令可以跑 Tool Calling Eval
- 报告能指出哪些意图容易选错工具
- 工具调用质量可量化

---

### Day 108：多轮状态 Eval

目标：

- 验证 Agent 在多轮对话中是否正确保留和更新状态

当天任务：

- 编写多轮对话评测样例
- 校验字段继承、字段覆盖、字段删除
- 校验追问后是否继续执行正确工具
- 输出状态变化对比

完成标准：

- 多轮修改不会污染旧状态
- 用户补充信息能正确进入后续工具输入
- 状态管理质量可被测试复现

---

### Day 109：失败恢复 Eval

目标：

- 验证工具失败时 Agent 是否能给出可控、可信的回答

当天任务：

- 构造工具超时、空结果、权限不足、参数缺失场景
- 校验是否进入正确 fallback
- 校验最终回答是否说明限制
- 校验 trace 是否记录错误

完成标准：

- 失败恢复不是人工判断，而是可自动评估
- Agent 不会在工具失败后编造结论
- 面试中能展示工程质量保障

---

### Day 110：Agent Eval 与 RAG Eval 报告

目标：

- 把评测结果沉淀为项目可展示材料

当天任务：

- 生成评测报告模板
- 输出关键指标：意图准确率、工具选择准确率、失败恢复通过率、多轮状态通过率、RAG Recall@K
- 总结主要失败样例和修复建议
- 更新 README 或面试指南

完成标准：

- 有可读的 Agent 与 RAG 评测报告
- 指标能支撑“这个 Agent 行为可控”的结论
- 后续优化有明确依据

---

### Day 111：Eval 接入测试体系

目标：

- 让核心 Agent 行为具备持续回归能力

当天任务：

- 将关键 eval 用例接入 pytest
- 区分快速回归和完整评测
- 为 CI 预留执行命令
- 控制测试耗时，避免影响日常开发

完成标准：

- 修改 Agent 逻辑后能快速发现工具选择退化
- CI 可运行最小评测集
- 完整评测可手动运行

---

### Day 112：第 17 周测试与文档

目标：

- 完成 Agent 质量评估闭环

当天任务：

- 补齐 eval 文档
- 整理失败样例
- 更新面试指南中 Agent Eval 与 RAG Eval 的回答
- 整理本周可演示命令

完成标准：

- 能说明为什么需要 Agent Eval
- 能展示 Tool Calling 质量指标
- 第 17 周能力可作为求职项目亮点

---

## 第 18 周：Agent 化前端完善与权限展示适配

周目标：

- 把第 12 周到第 17 周完成的 Agent Runtime、Tool Calling、Trace、Guardrail、RAG Eval 等后端能力接入前端展示
- 让前端从普通业务页面升级为可解释、可观测、可排障的 Agent 控制台
- 明确前端权限只负责入口和展示控制，真实权限边界仍由后端鉴权和数据库查询过滤保证

本周重点：

- 修复前端中文乱码和基础文案问题
- Agent 对话页展示 DeepSeek / 规则解析 / fallback 来源
- Agent 对话页展示 `agent_runtime`、工具调用、fallback、上下文合并信息
- 接入 `/agent/traces/{trace_id}`，支持执行链路复盘
- 增强 RAG 证据展示，能说明知识建议来自哪些片段
- 历史记录和管理员审计页增加 trace、runtime、权限相关展示
- 补充 `tenant_id`、`role`、`visibility` 等权限数据模型说明

完成标准：

- 前端能展示 Agent 的解析来源、执行链路、工具调用、fallback 和 RAG 证据
- 普通用户与管理员看到不同的调试深度和入口
- 管理员可以从审计记录复盘一次 Agent 执行过程
- 文档能说明 `tenant_id` 不是上传 ID，而是组织级权限隔离字段
- 前端构建通过，不破坏原有评估、推荐、比选、历史查询主流程

---

### Day 113：前端现状梳理与编码修复

目标：

- 先修复前端可读性问题，并明确 Agent 化改造入口

当天任务：

- 检查 `frontend/src` 下中文乱码文案
- 使用乱码特征字符扫描确认是否仍有残留，例如 `�`、`锛`、`鐩`、`杩`、`銆`
- 修复导航、按钮、表单、提示、空状态等基础中文文案
- 梳理 AgentPage、HistoryPage、AdminConversationsPage、KnowledgeAdvicePanel 的现有数据展示
- 确认 `/agent/query` 当前响应字段和前端类型差异
- 更新前端适配计划中的文件清单

完成标准：

- 前端主要页面中文显示正常
- `frontend/src` 下无明显中文乱码字符残留
- 前端 lint/build 通过，确认文案修复没有引入 TSX 或构建错误
- 明确哪些页面需要接入 Agent Runtime、Trace 和 RAG 证据
- 不改动后端业务逻辑
- 不在 Day113 新增 Agent Runtime、Trace、RAG 证据组件，相关功能进入 Day114 到 Day117

---

### Day 114：Agent 响应类型与解析来源展示

目标：

- 让前端能结构化识别 DeepSeek、规则解析和 fallback 解析

当天任务：

- 扩展 `frontend/src/types/agent.ts`
- 增加 `AgentRuntimeDebug` 类型
- 为 `parser_source` 增加展示映射：`rule`、`llm`、`llm_fallback_rule`
- 新增或内联实现 `ParserSourceBadge`
- 在 Agent 对话页展示 `intent`、`target_endpoint`、`parser_source`、`warnings`
- 对缺参和解析失败场景做用户可读提示

完成标准：

- 用户能看出当前解析来自 DeepSeek、规则，还是 DeepSeek 失败后的规则 fallback
- `warnings` 不再只藏在 JSON 中
- 旧响应没有 `agent_runtime` 时前端仍兼容

---

### Day 115：Agent Runtime 与工具调用面板

目标：

- 把后端 Agent Runtime 的关键字段变成可读调试信息

当天任务：

- 新增 `AgentRuntimePanel`
- 展示 `mode`、`trace_id`、`run_id`、`status`
- 展示 `plan_actions`、`tool_results`、`errors`
- 展示 `fallback_used`
- 增加 `ContextMergePanel`，展示 `modified_fields`、`invalidated_tools`、`field_sources`
- 失败或 fallback 时自动展开关键调试信息
- 只消费后端已有 `agent_runtime` 响应字段，不修改后端 Runtime 生成逻辑

完成标准：

- Agent 对话页能回答“Agent 计划做了什么、实际调用了什么工具、是否 fallback”
- 多轮修改时能看到哪些字段被覆盖、哪些工具结果失效
- Runtime 面板默认不干扰普通用户阅读主结论
- 旧响应缺少 `agent_runtime` 时，前端仍按兼容模式展示

---

### Day 116：Trace 查询与执行链路时间线

目标：

- 让一次 Agent 执行可以通过 `trace_id` 在前端复盘

当天任务：

- 新增 `frontend/src/types/trace.ts`
- 新增 `frontend/src/api/trace.ts`
- 封装 `GET /agent/traces/{trace_id}`
- 新增 `TraceTimeline` 组件
- 在 Agent 对话页从 `agent_runtime.trace_id` 提供 Trace 查询入口
- 按 `step_index` 展示 plan、tool_call、tool_result、error、fallback、final_response
- 支持展开 `input_summary`、`output_summary`、`metadata`
- 兼容旧响应：没有 `agent_runtime` 或 `trace_id` 时展示空状态，不影响主结论
- Trace 查询失败时展示提示，但不影响主对话结果
- 401 提示重新登录，404 统一提示 trace 不存在或当前账号无权访问，避免泄露其他用户 trace 是否存在
- 默认折叠 `input_summary`、`output_summary`、`metadata`，避免调试信息干扰业务结论

完成标准：

- 点击 `trace_id` 可以查看执行链路
- 工具调用、错误、fallback 都能在时间线里看到
- 前端不暴露不属于当前用户的 trace 数据
- 用户 A 不能通过输入或点击用户 B 的 `trace_id` 查看对方执行链路
- 没有 `trace_id` 的旧历史响应前端不报错
- `npm run lint`、`npm run build`、`python -m pytest tests/test_agent_trace_api.py tests/test_agent_trace.py` 通过

---

### Day 117：RAG 证据面板增强

目标：

- 让 RAG 建议具备可解释证据，而不是只展示自然语言建议

当天任务：

- 增强 `KnowledgeAdvicePanel`
- 明确兼容 RAG 数据来源：`composed.details.advice`、`composed.details.knowledge_snippets`、`composed.details.snippets`、`composed.details.retrieval_status`、`composed.details.retrieval_metadata`
- 展示 `retrieval_status`、`retrieval_message`、`retrieval_metadata`
- 扩展 `KnowledgeSnippet` 类型，支持 `knowledge_id`、`chunk_id`、`knowledge_type`、`chunk_type`
- 展示 `score`、`source`、`retriever`、`retrievers`
- 开发者模式展示 `metadata_boost`、`rerank_boost`、query rewrite 信息
- 对低置信或 fallback 检索展示保守提示
- 没有 RAG 数据时不展示面板；有 advice 但没有 snippets 时展示“暂无依据片段”空态
- metadata 缺失时开发者区正常降级，不影响业务结论
- 前端只展示后端已经按 `visibility`、`tenant_id`、`user_id` 过滤后的 RAG 结果，不在前端重做权限判断

完成标准：

- 用户能看到知识建议的依据片段
- 开发者能看到 RAG 召回来源和排序依据
- 前端明确提示 RAG 只做解释增强，不替代安全裁决
- fallback 或低置信检索有明显保守提示
- RAG 字段缺失、旧响应或空召回时前端不报错
- `npm run lint`、`npm run build`、`python -m pytest tests/test_rag_fallback.py tests/test_knowledge_retrievers.py tests/test_hybrid_knowledge_retriever.py tests/test_bm25_knowledge_store.py` 通过

---

### Day 118：历史记录、管理员审计与权限展示适配

目标：

- 把 Agent Runtime、Trace 和权限边界接入历史与审计页面

当天任务：

- 历史详情展示 `parser_source`、`context_used`、`trace_id`
- 历史详情从 `response.agent_runtime.trace_id` 提供 TraceTimeline 查询入口
- 管理员审计详情展示 runtime 摘要、失败类型、fallback 决策
- 管理员审计详情从 `response.agent_runtime.trace_id` 提供 TraceTimeline 查询入口
- 前端 `role` 类型收敛为 `user | admin | super_admin`
- 普通用户隐藏管理员入口
- 管理员页面说明只能作为入口控制，真实权限由后端保证
- 文档补充 `tenant_id`、`user_id`、`role`、`visibility` 的区别
- 复用 Day116 的 Trace API 和 TraceTimeline，不新增后端 trace 查询接口
- 不修改数据库权限模型，只做历史、审计和权限边界展示适配

完成标准：

- 普通用户只能看到自己的历史和 trace 入口
- 管理员能从审计页复盘 Agent 执行链路
- 权限模型说明清楚：数据库存归属字段，后端做查询过滤，前端做展示控制
- 历史页和管理员审计页不再只展示 trace 占位文案，而是可以直接查询时间线
- `npm run lint`、`npm run build`、`python -m pytest tests/test_conversation_history_api.py tests/test_admin_conversation_audit_api.py tests/test_agent_trace_api.py` 通过

---

### Day 119：第 18 周前端联调、构建与文档

目标：

- 完成 Agent 化前端适配闭环

当天任务：

- 跑通前端构建
- 联调 Agent 对话、Trace、RAG 证据、历史、管理员审计页面
- 用典型样例验证 DeepSeek 解析、规则 fallback、多轮上下文、工具失败、RAG 检索展示
- 更新 `Agent化后前端与权限数据模型适配改造计划`
- 整理剩余问题清单，留给第 19 到第 21 周产品化功能开发，以及第 22 周 CI/CD 和展示收尾处理

完成标准：

- 前端主要演示路径稳定可运行
- Agent Runtime、Trace、RAG evidence 和 fallback 都能被前端展示
- 权限展示和数据库权限设计在文档中保持一致
- 第 18 周能力可作为“Agent 工程化可视化”亮点

---

## 第 19 周：可配置规则集与规则命中解释

周目标：

- 把当前代码内置规则升级为可配置、可校验、可解释、可复盘的规则集体系
- 保证默认规则仍然兼容现有评估结果，用户规则只在校验通过后参与安全裁决
- 让评估结果和历史记录都能说明“实际值为什么命中某条阈值规则”

本周重点：

- 新增 rule set 与 rule item 数据模型
- 建立规则字段白名单、operator 白名单和阈值范围校验
- 默认任务类型规则转换为系统默认规则集快照
- `/cruise/evaluate` 支持 `rule_set_id`
- 评估结果新增 `rule_hits`
- 历史记录保存 `rule_snapshot_json` 和 `rule_hits_json`
- 前端新增规则集管理和命中解释展示

完成标准：

- 不传规则集时，默认规则结果保持兼容
- 传入合法规则集时，评估按用户规则执行
- 非法 metric、operator、decision、阈值范围和冲突阈值会被拒绝
- 历史复盘能解释当时使用的规则快照和命中规则
- 普通用户不能修改他人的私有规则集，管理员可以管理公共规则集

---

### Day 120：规则集数据模型与迁移设计

目标：

- 明确规则集和规则项的数据库结构，为后续接口和评估接入打基础

当天任务：

- 新增 `rule_sets` 表设计
- 新增 `rule_items` 表设计
- 定义 `draft`、`active`、`archived` 状态
- 设计 `owner_user_id`、`tenant_id`、`visibility` 权限字段
- 编写 Alembic migration
- 补充规则集 schema 和响应类型

完成标准：

- 数据库能保存规则集和规则项
- 规则集具备版本、状态、归属和可见性字段
- schema 能支撑后续 CRUD、校验和评估接入

---

### Day 121：规则字段白名单与校验器

目标：

- 防止用户配置非法规则进入安全裁决链路

当天任务：

- 定义 metric 白名单：`weather_text`、`precip`、`pop`、`wind_speed`、`wind_scale`、`humidity`、`visibility`、`warning_level`、`warning_type`
- 定义 operator 白名单：优先支持 `>=`、`==`、`in`
- 定义 decision 枚举：`适飞`、`慎飞`、`禁飞`
- 增加数值范围校验
- 增加 unit 与 metric 匹配校验
- 增加同一指标慎飞和禁飞阈值冲突校验
- 编写规则校验单元测试

完成标准：

- 非法字段、非法操作符、非法结论和明显脏数据会被拒绝
- 规则集至少包含一条启用规则
- 冲突阈值能被明确报错

---

### Day 122：规则集管理 API

目标：

- 提供规则集的后端管理入口

当天任务：

- 实现 `GET /rule-sets`
- 实现 `POST /rule-sets`
- 实现 `GET /rule-sets/{rule_set_id}`
- 实现 `PATCH /rule-sets/{rule_set_id}`
- 实现规则项新增、编辑、删除接口
- 实现 `POST /rule-sets/{rule_set_id}/validate`
- 实现 `POST /rule-sets/{rule_set_id}/activate`
- 增加用户归属和管理员权限校验

完成标准：

- 用户可复制、编辑、校验和启用自己的规则集
- 普通用户不能修改他人的私有规则集
- 管理员可维护公共规则集

---

### Day 123：默认规则迁移为系统规则集

目标：

- 保持现有规则引擎兼容，同时为用户规则接入统一执行结构

当天任务：

- 将当前任务类型差异化阈值整理为系统默认规则集
- 为巡航、巡检、悬停拍摄、测绘生成默认规则项
- 增加系统默认规则集初始化脚本或 seed 数据
- 调整规则引擎读取规则项而不是只读取硬编码配置
- 保留旧配置 fallback，降低迁移风险

完成标准：

- 不传 `rule_set_id` 时仍使用系统默认规则
- 默认规则输出与改造前保持一致
- 规则引擎具备统一规则项执行入口

---

### Day 124：评估结果新增 rule_hits

目标：

- 让每次风险判断都能结构化说明命中了什么规则

当天任务：

- 扩展评估响应 schema
- 增加 `rule_hits` 生成逻辑
- 记录 `actual_value`、`operator`、`threshold`、`unit`、`decision`、`label`、`risk_tag`
- 处理逐小时规则命中与整体结论汇总关系
- 保持旧字段 `risk_factors` 兼容
- 增加规则命中单元测试和接口测试

完成标准：

- 指定规则集后，评估结果包含完整 `rule_hits`
- 前端仍能消费旧的 `risk_factors`
- 命中解释能表达“实际值 vs 阈值”

---

### Day 125：历史规则快照与复盘查询

目标：

- 避免规则修改污染历史评估解释

当天任务：

- 扩展历史评估表字段
- 保存 `rule_set_id`、`rule_set_version`、`rule_snapshot_json`、`rule_hits_json`
- 调整 `/history` 或任务历史详情响应
- 增加历史快照查询测试
- 验证规则修改后旧历史仍显示旧规则命中解释

完成标准：

- 每次评估都保存当时使用的规则快照
- 历史详情能展示当时命中的规则
- 规则后续修改不会改变历史复盘结果

---

### Day 126：规则集前端管理与第 19 周联调

目标：

- 完成规则集闭环的前后端可演示版本

当天任务：

- 新增规则集管理页面
- 支持复制系统默认规则集
- 支持表格编辑规则项
- 支持保存草稿、校验、启用规则集
- 在评估结果页展示 `rule_hits`
- 在历史详情页展示规则快照和命中解释
- 跑通后端测试和前端构建

完成标准：

- 用户可以配置规则、启用规则并参与评估
- 评估和历史页面能展示规则命中解释
- 第 19 周能力可作为“安全裁决可配置、可解释、可复盘”亮点

---

## 第 20 周：RAG 知识库数据库化与后台管理

周目标：

- 把静态 JSON 知识库升级为数据库管理的知识源
- 保持现有 BM25、Embedding、Hybrid Retrieval、RAG Eval 主链路可复用
- 让知识具备审核、启用、禁用、有效期、权限过滤和索引重建能力

本周重点：

- 新增 `knowledge_documents` 表和可选 `knowledge_index_jobs` 表
- 编写 JSON 知识导入脚本
- 新增管理员知识 CRUD
- 新增审核、启用、禁用和有效期管理
- 新增索引重建接口和任务状态记录
- 检索器读取数据库导出的 approved、active、未过期知识
- 管理后台展示知识列表、表单和索引任务状态

完成标准：

- 管理员可以维护知识库内容
- JSON 知识可以完整导入数据库
- inactive、rejected、expired、越权知识不会被召回
- 知识变更后可以重建索引
- RAG Eval 结果不低于当前基线

---

### Day 127：知识库数据模型与迁移

目标：

- 建立数据库化知识源的核心表结构

当天任务：

- 新增 `knowledge_documents` 表设计
- 复用 `knowledge_type`、地域、任务类型、风险标签、预警类型、预警等级、可见性、租户、用户、版本和有效期字段
- 新增 `review_status`、`is_active`、`index_dirty`
- 设计 `knowledge_index_jobs` 表
- 编写 Alembic migration
- 补充知识管理 schema

完成标准：

- 数据库能保存可治理的知识源
- 字段能支撑权限过滤、时效过滤、审核过滤和索引重建

---

### Day 128：JSON 知识导入脚本

目标：

- 将现有 `data/knowledge/advice_rules.json` 平滑迁移到数据库

当天任务：

- 新增 `scripts/import_knowledge_json.py`
- 读取并校验 JSON 知识字段
- 写入 `knowledge_documents`
- 设置 `review_status=approved`、`is_active=true`
- 标记 `index_dirty=true`
- 增加重复导入处理策略
- 编写导入脚本说明

完成标准：

- 现有 JSON 知识可以完整导入数据库
- 导入结果可重复执行且不会产生明显脏数据
- 导入后能触发后续索引重建

---

### Day 129：管理员知识 CRUD API

目标：

- 提供知识库后台管理的后端接口

当天任务：

- 实现 `GET /admin/knowledge`
- 实现 `POST /admin/knowledge`
- 实现 `GET /admin/knowledge/{knowledge_id}`
- 实现 `PATCH /admin/knowledge/{knowledge_id}`
- 实现 `DELETE /admin/knowledge/{knowledge_id}` 或软删除
- 实现 `POST /admin/knowledge/{knowledge_id}/status`
- 增加管理员权限校验
- 增加接口测试

完成标准：

- 管理员可新增、编辑、禁用和调整审核状态
- 普通用户不能访问管理员知识管理接口
- 知识变更会标记 `index_dirty`

---

### Day 130：索引重建流程

目标：

- 让数据库知识源能生成现有 RAG 检索器可消费的索引

当天任务：

- 实现 `POST /admin/knowledge/reindex`
- 实现 `GET /admin/knowledge/index-jobs`
- 从数据库读取 approved、active、未过期知识
- 生成现有 BM25、Embedding、Hybrid 索引文件
- 记录 `document_count`、`chunk_count`、`status`、`error_message`
- 索引成功后清理 `index_dirty`

完成标准：

- 管理员可以手动重建索引
- 索引任务状态可查询
- 检索器仍然消费稳定的索引文件，不需要重写上层接口

---

### Day 131：RAG 检索过滤与回归

目标：

- 确认数据库化后 RAG 权限、时效和业务过滤不退化

当天任务：

- 调整索引构建的数据源过滤
- 验证 `review_status`、`is_active`、`effective_at`、`expires_at`
- 验证 `visibility`、`tenant_id`、`user_id` 权限过滤
- 验证 `task_type`、region、risk_tags 元数据过滤
- 跑 RAG Eval 快速集
- 对比当前基线指标

完成标准：

- 不该召回的知识不会进入候选集
- 权限和有效期过滤在数据库源与索引侧保持一致
- RAG Eval 不低于当前基线

---

### Day 132：知识库管理前端

目标：

- 提供可演示的知识后台维护页面

当天任务：

- 新增知识库管理页面
- 展示知识列表、状态、有效期、来源和索引状态
- 新增知识编辑表单
- 支持启用、禁用和审核状态调整
- 支持重建索引按钮
- 展示最近索引任务状态
- 对普通用户隐藏管理员入口

完成标准：

- 管理员可以通过页面维护知识库
- 知识变更和索引重建状态可见
- 普通用户不会看到后台知识管理入口

---

### Day 133：RAG 数据库化联调与文档

目标：

- 完成知识库数据库化闭环

当天任务：

- 联调知识新增、编辑、禁用、审核、重建索引、检索召回
- 验证规则命中 `risk_tags` 与 RAG 检索联动
- 补充数据库知识库管理文档
- 补充 RAG Eval 回归说明
- 整理剩余问题清单

完成标准：

- 数据库知识管理、索引重建、RAG 检索和前端后台可完整演示
- RAG 明确只做解释增强，不替代规则裁决
- 第 20 周能力可作为“企业 RAG 数据治理与后台化”亮点

---

## 第 21 周：任务单与任务状态管理

周目标：

- 把零散对话、评估、推荐、规则快照、RAG 证据和 Trace 聚合到一个低空作业任务单下
- 让用户能围绕一次未来作业计划进行创建、评估、推荐、选择窗口、复核、完成或取消
- 保持任务单是业务聚合层，不替代 Conversation History 和评估历史

本周重点：

- 新增 `mission_tasks` 表
- Conversation History 和评估历史关联 `task_id`
- 新增任务 CRUD 和状态流转接口
- 新增任务评估、推荐、选择窗口、执行前复核接口
- Agent 支持创建任务、评估任务、推荐窗口、选择窗口和复核任务
- 前端新增任务列表和任务详情页
- 任务详情聚合对话、评估快照、规则快照、RAG evidence 和 Trace

完成标准：

- 用户可以按任务管理相关对话和评估
- 任务状态可追踪，非法状态流转会被拒绝
- 执行前复核会重新拉取天气并生成新的天气和规则快照
- 普通用户和管理员权限边界清晰

---

### Day 134：任务单模型与状态机设计

目标：

- 建立任务单作为业务聚合对象的基础结构

当天任务：

- 新增 `mission_tasks` 表设计
- 定义 `draft`、`evaluated`、`scheduled`、`recheck`、`completed`、`cancelled` 状态
- 设计 `selected_window_json`、`latest_decision`、`latest_request_id`、`latest_trace_id`、`latest_conversation_id`
- 设计 `conversation_records.task_id` 和 `task_requests.task_id` 关联
- 编写 Alembic migration
- 补充任务单 schema

完成标准：

- 数据库能保存任务单和关键关联字段
- 状态机边界清晰，后续接口可按状态校验

---

### Day 135：任务 CRUD 与状态接口

目标：

- 提供任务单基础管理能力

当天任务：

- 实现 `GET /tasks`
- 实现 `POST /tasks`
- 实现 `GET /tasks/{task_id}`
- 实现 `PATCH /tasks/{task_id}`
- 实现 `PATCH /tasks/{task_id}/status`
- 增加状态流转校验
- 增加用户归属和管理员审计权限校验
- 编写任务接口测试

完成标准：

- 创建任务后状态为 `draft`
- 普通用户只能查看和修改自己的任务
- 已取消和已完成任务不能继续执行排期类操作

---

### Day 136：任务评估、推荐与窗口选择

目标：

- 把现有评估和推荐能力接入任务单上下文

当天任务：

- 实现 `POST /tasks/{task_id}/evaluate`
- 实现 `POST /tasks/{task_id}/recommend`
- 实现 `POST /tasks/{task_id}/select-window`
- 将评估结果写入 `latest_request_id`、`latest_trace_id`、`latest_decision`
- 选择窗口后写入 `selected_window_json`
- 评估后状态变为 `evaluated`
- 选择窗口后状态变为 `scheduled`

完成标准：

- 用户可以在任务详情内完成风险评估和窗口推荐
- 任务状态会随操作自动更新
- 评估历史仍然保留独立记录，并通过 `task_id` 关联任务

---

### Day 137：执行前复核与快照更新

目标：

- 支持任务执行前重新确认天气、规则和风险结论

当天任务：

- 实现 `POST /tasks/{task_id}/preflight-check`
- 重新拉取天气并执行规则评估
- 保存新的天气快照、规则快照、rule hits 和 trace
- 更新任务最新结论和状态
- 禁止已取消任务继续复核
- 增加复核接口测试

完成标准：

- 执行前复核会产生新的评估记录和快照
- 任务详情能区分计划时评估和复核评估
- 高风险复核结果不会被旧推荐窗口覆盖

---

### Day 138：Agent 任务单意图接入

目标：

- 让自然语言入口可以操作任务单

当天任务：

- 扩展 Agent intent：创建任务、评估任务、推荐任务窗口、选择窗口、执行前复核
- 扩展参数解析，支持任务标题、目的、计划日期、候选地点、选择第几个窗口
- 将任务工具注册到 Tool Registry
- 在 Agent State 中保存当前任务上下文
- 增加多轮样例：创建任务、评估、推荐、选择、复核

完成标准：

- 用户可以说“帮我创建一个深圳湾明天下午巡检任务”
- 用户可以继续说“评估一下这个任务”“就选第一个窗口”“执行前再检查一次”
- Agent 不会绕过任务归属和权限校验

---

### Day 139：任务列表与任务详情前端

目标：

- 提供任务单的前端管理和演示入口

当天任务：

- 新增任务列表页面
- 新增任务详情页面
- 展示当前状态、地点、任务类型、计划时间、最近结论
- 展示推荐窗口和已选窗口
- 展示相关对话、历史评估快照、规则快照、RAG evidence 和 Trace 入口
- 增加评估、推荐、选择窗口、复核、完成、取消操作按钮

完成标准：

- 用户可以通过页面管理一个低空作业任务
- 任务详情能集中展示对话、评估、规则、RAG 和 Trace
- 前端状态按钮不会暴露非法操作入口

---

### Day 140：任务单联调、权限回归与文档

目标：

- 完成任务单与状态管理闭环

当天任务：

- 联调任务创建、评估、推荐、选择窗口、复核、完成、取消
- 验证普通用户、管理员审计和越权访问场景
- 验证 Conversation History、评估历史和 Trace 的 `task_id` 关联
- 补充任务单接口文档和状态流转图
- 更新 README 或面试指南中的任务单业务价值说明
- 整理进入第 22 周 CI/CD 和展示收尾的问题清单

完成标准：

- 任务单主链路可完整演示
- 权限边界和状态边界通过回归测试
- 第 21 周能力可作为“业务任务状态管理与 Agent 上下文聚合”亮点

---

## 第 22 周：CI/CD、展示收尾与求职材料完善

### Day 141：GitHub Actions 后端测试

目标：

- 建立最小 CI，保证后端核心能力可持续回归

当天任务：

- 配置 GitHub Actions
- 运行后端单元测试
- 缓存依赖
- 输出测试结果

完成标准：

- push 后能自动运行后端测试
- CI 能覆盖 Agent Runtime、权限过滤和核心规则

---

### Day 142：Agent Eval 与 RAG Eval 接入 CI

目标：

- 将最小 Agent Eval 和 RAG Eval 纳入质量门禁

当天任务：

- 选择快速 Agent Eval 子集
- 选择快速 RAG Eval 子集
- 接入 CI
- 设置失败阈值
- 文档说明本地与 CI 的运行方式

完成标准：

- 工具选择退化能被 CI 发现
- RAG 召回退化能被 CI 发现
- 完整 eval 与快速 eval 边界清晰

---

### Day 143：前端构建与兼容检查

目标：

- 确认 Agent 化前端改造没有破坏现有用户界面

当天任务：

- 运行前端构建
- 检查关键页面字段兼容
- 检查 trace 和 RAG evidence 展示入口
- 检查普通用户和管理员入口差异
- 记录前后端联调问题

完成标准：

- 前端能正常构建
- 主要演示路径可运行
- trace 和 RAG evidence 信息可选展示，不影响普通用户

---

### Day 144：README 重构

目标：

- 让项目定位从“低空业务 workflow”升级为“面向业务场景的 AI Agent 工程项目”

当天任务：

- 更新项目简介
- 更新架构说明
- 增加 Agent Runtime、Tool Calling、Trace、Eval、Guardrail、Hybrid RAG、Agent 化前端展示章节
- 增加权限模型说明：`user_id`、`tenant_id`、`role`、`visibility`
- 增加本地运行和测试命令

完成标准：

- README 能让面试官快速理解项目亮点
- 技术栈与岗位能力要求对应清楚
- 前端展示、后端 Agent Runtime 和权限设计能形成闭环

---

### Day 145：项目面试指南最终版

目标：

- 把项目中的工程能力转化为面试可表达的技术答案

当天任务：

- 更新 workflow 与 Agent 区别
- 更新 Tool Calling 设计
- 更新 Trace 与日志设计
- 更新 Agent Eval 与 Guardrail
- 更新 RAG 数据治理、BM25、Embedding、Hybrid Retrieval、Rerank 和 RAG Eval
- 增加 Agent 化前端、权限展示和 `tenant_id` 数据隔离问答

完成标准：

- 面试指南不再只围绕 RAG，也不会弱化 RAG
- 能覆盖 Agent 开发工程师常见追问
- 回答能结合项目实现而不是泛泛而谈

---

### Day 146：端到端演示脚本

目标：

- 准备一套可在面试中稳定演示的场景

当天任务：

- 设计评估类演示
- 设计推荐类演示
- 设计多轮修改演示
- 设计工具失败和 trace 演示
- 设计 Hybrid RAG 召回对比演示
- 设计前端 Agent Runtime 和权限展示演示
- 记录预期输入输出

完成标准：

- 演示脚本稳定可复现
- 每个演示都对应一个工程亮点
- 面试时能在 5 到 10 分钟内讲清楚

---

### Day 147：最终验收

目标：

- 完成求职版本项目验收

当天任务：

- 跑通后端测试
- 跑通前端构建
- 跑通 Agent Eval 快速集
- 跑通 RAG Eval 快速集
- 检查 README、面试指南和改进计划一致性
- 检查 Agent 化前端展示路径
- 整理最终问题清单

完成标准：

- 项目具备可运行、可解释、可评估、可展示四个特征
- Agent 和 RAG 两条主线都有工程化落点
- 前端能展示 Agent Runtime、Trace、RAG evidence 和权限边界
- 后续优化方向清晰，但当前版本已经能用于求职展示

## 5. 关键里程碑

### 里程碑 1：后端基础跑通
时间：
- 第 1 周结束

判定标准：
- FastAPI 可运行
- 天气 API 可调用
- 输入校验完整

### 里程碑 2：核心判断闭环完成
时间：
- 第 2 周结束

判定标准：
- 逐小时天气提取、预警提取、巡航判断全部本地化完成
- 输出结果与现有原型一致

### 里程碑 3：功能增强完成
时间：
- 第 3 周结束

判定标准：
- 最佳执行时间推荐可用
- 多地点任务比选可用
- 历史任务复盘可用
- 任务类型差异化规则可用

### 里程碑 4：Agent 主流程完成
时间：
- 第 4 周结束

判定标准：
- 支持自然语言输入
- 支持会话记忆
- 支持统一任务编排

### 里程碑 5：项目可展示可部署
时间：
- 第 5 周结束

判定标准：
- 接入 RAG
- 完成 Docker 部署
- GitHub 仓库可公开展示

### 里程碑 6：前端展示控制台完成
时间：
- 第 6 周结束

判定标准：
- 支持 Agent 对话页面
- 支持评估、推荐、比选、历史查询页面
- 前端可以完整展示后端核心能力

### 里程碑 7：求职展示版本完成
时间：
- 第 7 周结束

判定标准：
- 前后端可通过 Docker 本地启动
- README 和部署说明完整
- 面试演示路径清晰

### 里程碑 8：DeepSeek 自然语言解析增强完成
时间：
- 第 8 周结束

判定标准：
- `/agent/query` 支持 DeepSeek 结构化解析
- 当前规则解析作为 fallback 保留
- 单地点评估、窗口推荐、多地点比选三类自然语言输入均可解析
- 响应中能区分 `rule`、`llm` 和 fallback 来源
- 文档中明确说明 LLM 不参与安全规则判断

### 里程碑 9：多用户登录与记忆持久化完成
时间：
- 第 9 周结束

判定标准：
- 支持用户注册、登录、JWT 鉴权和 `/auth/me`
- `/agent/query` 使用真实登录用户写入历史、Session 和 Profile
- 普通用户只能查看自己的历史任务、Session 和 Profile
- Session Memory 可持久化恢复，不同用户上下文互不污染
- Profile Memory 从 `default_user` 升级为真实用户长期偏好

### 里程碑 10：管理员审计与多用户前端完成
时间：
- 第 10 周结束

判定标准：
- 普通用户和管理员登录后看到不同页面入口
- 管理员可以查看用户列表、全局任务审计和系统统计
- 普通用户访问 `/admin/*` 返回 403
- 被禁用用户不能继续登录或调用受保护接口
- 文档和面试话术能说明数据隔离、权限边界和管理员审计价值

### 里程碑 11：RAG 数据治理基础完成

验收点：

- 知识库支持 `knowledge_type`、地域、权限、租户、用户、版本、时效和审核状态
- RAG 检索能按用户权限和业务场景过滤
- 能说明企业 RAG 中权限隔离、时效控制和地域适用性的设计

---

### 里程碑 12：Agent Runtime 基础完成

验收点：

- 核心业务能力被注册为标准工具
- Agent State 能表达多轮任务状态
- Agent Loop 能按意图选择工具，而不是固定执行完整 workflow

---

### 里程碑 13：Trace、日志与失败恢复完成

验收点：

- 每次 Agent 执行都有 trace_id 和工具调用链路
- 工具失败被分类并有降级策略
- 日志支持排障，同时避免泄露敏感输入

---

### 里程碑 14：混合型业务编排完成

验收点：

- 同一自然语言入口支持查询、评估、推荐、比选、追问和多轮修改
- RAG 作为可选工具参与编排，而不是固定链路
- 修改任务条件后能只重跑受影响工具

---

### 里程碑 15：Guardrail 与安全边界完成

验收点：

- 输入、工具调用和最终回答都有安全边界
- Agent 不能绕过工具权限、租户隔离和用户隔离
- 高风险输出具备保守表达、拒答和追问策略

---

### 里程碑 16：Hybrid RAG 检索增强完成

验收点：

- RAG 从 TF-IDF baseline 升级为 BM25 + Embedding + Hybrid Retrieval
- 支持 metadata filter、chunk 策略、规则 rerank、query rewrite 和低置信 fallback
- RAG Eval 能对比 TF-IDF、BM25、Embedding、Hybrid 的召回效果

---

### 里程碑 17：Agent Eval 与 RAG Eval 完成

验收点：

- 有可版本管理的 Agent Eval 与 RAG Eval 数据集
- 能评估工具选择准确率、多轮状态正确率和失败恢复通过率
- Eval 结果能结合 RAG 召回指标生成报告并指导后续优化

---

### 里程碑 18：Agent 化前端完善完成

验收点：

- 前端能展示 Agent Runtime、Trace、工具调用、fallback 和 RAG evidence
- 普通用户和管理员具备不同的入口和调试信息展示深度
- 前端权限展示与数据库权限模型文档保持一致

---

### 里程碑 19：可配置规则集与规则命中解释完成

验收点：

- 规则集支持草稿、启用、归档、版本、归属和可见性
- 用户规则经过字段白名单、operator、阈值范围和冲突校验后才能启用
- 评估结果包含 `rule_hits`，历史记录保存规则快照和命中快照
- 默认规则保持兼容，用户规则可覆盖评估阈值但不能绕过后端安全校验

---

### 里程碑 20：RAG 知识库数据库化与后台管理完成

验收点：

- 知识库从静态 JSON 升级为 `knowledge_documents` 数据库管理
- 管理员可以新增、编辑、禁用、审核知识并触发索引重建
- RAG 检索过滤支持权限、租户、时效、审核状态、地域、任务类型和风险标签
- RAG Eval 回归结果不低于当前基线

---

### 里程碑 21：任务单与任务状态管理完成

验收点：

- 用户可以创建任务单，并围绕任务完成评估、推荐、选择窗口、执行前复核、完成或取消
- Conversation History、评估历史、规则快照、RAG evidence 和 Trace 能按 `task_id` 聚合
- 任务状态流转可校验，非法状态操作会被拒绝
- 普通用户和管理员权限边界清晰

---

### 里程碑 22：CI/CD 与求职展示版本完成

验收点：

- GitHub Actions 能运行后端测试、最小 Agent Eval 和最小 RAG Eval
- README、面试指南、演示脚本与项目实现保持一致
- 项目具备可运行、可解释、可评估、可展示四个特征


---

## 6. 建议的执行策略

### 6.1 每天开发顺序
建议每天按以下节奏：

1. 前 30 分钟  
   回顾昨天进度，明确今天唯一主目标

2. 中间 3 到 3.5 小时  
   只做一个模块，不要多线程切换

3. 最后 30 到 60 分钟  
   测试、提交代码、写当天记录

### 6.2 每周固定动作
每周最后一天做三件事：
- 清理代码
- 补文档
- 记录下周要做的第一件事

### 6.3 对后续增强项的处理原则

对于以下内容，不要在当前主链路尚未稳定时提前做重型实现：

- 大规模实时政策爬虫和自动知识库维护
- 重型向量数据库、复杂混合检索和企业级知识平台
- 复杂多 Agent 协作
- 强依赖 LLM 的黑盒 Planner
- 过度前端美化
- 先把第 12 周到第 22 周的 Agent Runtime、Tool Calling、Trace、Guardrail、Hybrid RAG、Agent Eval/RAG Eval、Agent 化前端展示、产品化功能和 CI/CD 做扎实

### 6.4 开发顺序不要打乱

不要一开始就上：

- 强化学习
- 复杂多 Agent
- 过度前端美化
- 完整用户权限体系
- 重型向量数据库或实时政策爬虫

正确顺序是：

- 核心链路
- 规则引擎
- 功能增强
- Agent 编排
- Session Memory
- 长期用户体系
- 管理员审计和多用户前端
- RAG 数据治理基础
- Tool Registry、Agent State 和 Agent Loop
- Trace、日志和工具失败恢复
- 混合型业务编排和多轮修改
- Guardrail、安全边界和权限回归
- Hybrid RAG、RAG Eval 和召回优化
- Agent Eval、RAG Eval 和 Tool Calling 质量评估
- Agent 化前端完善、Trace 可视化、RAG evidence 展示和权限展示适配
- CI/CD、项目文档和求职展示收尾

---

## 7. 一句话执行建议

这次重构最重要的不是“技术点尽量多”，而是：

`每周都交付一个能运行、能展示、能写进简历的阶段成果。`

当前应该优先保证：
- 主链路稳定
- 模块边界清晰
- 易变层可替换

在这个基础上，再逐步补：
- 大模型自然语言解析与 LLM 解释层
- Redis 会话记忆
- 用户体系与长期偏好
- 管理员审计
- 前端展示控制台
- RAG 元数据、混合检索和空召回兜底
- Trace、Guardrail、Hybrid RAG、Agent Eval 和 RAG Eval
- Agent 化前端展示、Trace 时间线、RAG evidence 和权限模型说明
- Tool Calling 标准化和 CI/CD 质量门禁

只要按这个节奏推进，后端 5 周内完成核心闭环，第 6 到第 8 周补齐前端展示和 LLM 解析，第 9 到第 10 周补齐多用户、持久化记忆和管理员审计，第 11 到第 17 周补齐 RAG 数据治理、Agent Runtime、Trace、Guardrail、Hybrid RAG 和 Eval，第 18 周补齐 Agent 化前端可视化与权限展示，第 19 到第 21 周补齐可配置规则集、RAG 知识库后台化和任务单状态管理，第 22 周补齐 CI/CD 和求职展示收尾后，它就不再是“比赛作品重写”，而是一套更贴近 AI Agent 开发工程师岗位要求的求职项目。



